# TaskBook

A tiny task list for the command line.

## Install

```
pip install -r requirements.txt
```

## Usage

Add a task:

```
python taskbook.py new "write report" -p 2
```

Show your tasks:

```
python taskbook.py show
```

That's it — TaskBook keeps everything in a local file.
