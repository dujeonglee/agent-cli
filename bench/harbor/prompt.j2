{{ instruction }}

---
You are running non-interactively inside the task's container — nobody can answer questions, so never use `ask`; decide and proceed. Work in the current directory. Use `shell` to inspect the environment and to verify your work (run the program, run tests, check outputs). When the task is fully done, call `complete` with a short summary of what you did.
