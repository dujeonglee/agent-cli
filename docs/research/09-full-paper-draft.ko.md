# Coagora: 단일 LLM 에이전트 세션의 동기적 다중 사용자 공유를 위한 동시성 계약을 통한 멀티플레이어 코딩 에이전트

> **한국어 번역본** (v0.8 대응, 2026-08-06). v0.8 은 측정 개정이 아니라 **구조 개정**이다: 이제 논문은 **하나의 시스템**을 제시한다. 이전 초안들은 Coagora 를 두 번 지어진 시스템으로 서술하고 수치의 절반 가량을 별도의 탐색 단계 코드베이스에서 가져왔다. 그 코드베이스는 동결되었고 더 이상 참조하지 않으며, 아래의 모든 수치는 이 리포지토리에 커밋된 원시 데이터(`bench/multiuser/out/`)에서 재도출했다. 비교 기준도 그만큼 날카로워졌다: 직렬 계약은 허수아비가 아니라 **이 시스템이 이미 출하하던 모드**, 즉 오늘도 기본값으로 남아 있는 다중 뷰어 공유 워커이고, 병렬 계약은 백지에서 설계된 것이 아니라 작동하는 시스템의 진화로 제시된다. v0.8 신규: 늦은 합류자 증분 재생 실험(§6.5), 충돌 단위 락의 동기를 인용이 아니라 이 시스템 자체의 데이터로 세우는 측정(§6.4), HOL 그리드의 L = 30 s 셀(§6.1). §5·§6.4·§6.5 는 신규 또는 전면 재작성이고, §6.6~§6.10 은 내용은 유지하되 수치를 재도출했다. 이전 초안: v0.7 은 스타일 정비(산문 em-dash 제거, 약어 첫 사용 시 풀네임, 평가 질문 RQ 표기, 참고문헌 인용 순 번호), v0.6 은 현재 호스트에서의 재실측, v0.3~v0.5 는 설계 공간·붕괴 경계·수명주기/실모델 실험, 그리고 v0.8 이 제거하는 초기 탐색 단계의 결과를 추가했다.
> 정본은 영문 초안 `09-full-paper-draft.md` 이며, 본 번역본은 검토·논의용이다. 수치·인용 번호·절 구조는 정본과 1:1 로 대응한다. 투고 원고는 영문 정본에서 만든다.
> 저자용 상태 노트는 `[NOTE: …]`/`[TODO: …]` 로 표기되며 제출 전 제거한다.
> ✔ 표시 수치는 `bench/multiuser/out/` 의 커밋된 원시 데이터가 근거다.

---

## 초록 (Abstract)

AI 코딩 에이전트는 파일을 쓰고, 셸을 실행하고, 풀 리퀘스트를 열지만, 배포된 모든 에이전트는 단일 사용자를 전제한다. 에이전트를 함께 쓰려는 팀은 메신저 통합(Claude Tag, OpenAI Codex, GitHub Copilot, Devin, Slack 의 Cursor)으로 몰리는데, 이들은 공통 아키텍처를 공유한다: @멘션이 에이전트를 트리거하고, 스레드 이력이 컨텍스트로 흡수되며, 작업은 분리된 클라우드 샌드박스에서 실행되고, 풀 리퀘스트 링크가 돌아온다. 이 비동기-위임 설계에는 에이전트가 도는 동안의 개입 지점이 없고, 여러 사용자가 동시에 지시할 때의 조정 프로토콜도 없다. 명백한 대안, 즉 여러 사용자가 하나의 라이브 에이전트 세션을 공유하는 방식은 실무자들이 강요된 선택으로 취급하는 문제에 부딪힌다: 추론을 직렬화하거나(내 질문이 당신의 15초짜리 빌드 뒤에서 기다린다), 사용자별로 세션을 포크하거나(협업을 매력 있게 만든 공유 컨텍스트를 잃는다).

우리는 제3의 선택지가 있음을 보인다. 우리는 (i) 다중 사용자 코딩 에이전트 상호작용의 4축 설계 공간(*상태 소재(state locus)*, *동시성 계약(concurrency contract)*, *귀속 단위(attribution unit)*, *개입 지점(intervention point)*)을 정식화하고 아홉 개 기존 시스템을 그 안에 배치하며; (ii) 이전까지 비어 있던 좌표를 차지하는 동시성 계약 **병렬 추론, 직렬 부수효과(parallel inference, serialized side effects)** 를 제안한다: 동시에 도착한 사용자 입력은 하나의 공유 대화 컨텍스트 위에서 독립적으로 동시 비행하는 턴으로 추론되고, 모든 부수효과(파일 쓰기, 도구 실행, 컨텍스트 커밋)는 충돌 단위 계층 락을 통과한다; 그리고 (iii) 직렬 계약으로 이미 다중 동시 뷰어에게 공유 세션을 출하하던 오픈소스 에이전트 **Coagora** 를 그 계약의 구현으로 진화시킨 뒤, 세 계약 모두를 하나의 하네스로 실측한다.

이 평가는 **한 시스템 내부의 비교**이며, 바로 그 점이 날을 세운다: 직렬 팔은 이 시스템 자신의 프로덕션 경로이자 지금도 기본값이고, 거부 팔은 배포된 다중 사용자 봇 프레임워크들의 관행을 같은 코드 위의 게이트로 구현한 것이다. 240회 실측에서, 병렬 계약 하의 두 번째 사용자의 첫 토큰까지의 시간(time-to-first-token, TTFT)은 첫 번째 사용자의 작업 길이로부터 독립이었다: 작업 길이에 대한 TTFT 회귀 기울기는 0.00 이었고, 선입선출(first-in-first-out, FIFO) 직렬화는 1.03, 거부-후-재시도 게이트는 1.03 이었다. 거부 게이트는 직렬보다 나은 적이 없다; 직렬의 대기를 전부 치른 뒤 위상 의존 세금(여기서는 79 ms~232 ms)을 더 내고, 재시도 기계를 모든 클라이언트에 떠넘긴다. 효과 락을 제거하자 표본 추출된 동시 동일-파일 쓰기의 8.2%에서 무결성 위반이 발생했고, 계약은 병합도 롤백도 낙관적 동시성 제어도 없이 이를 0%로 만든다. 이어서 우리는 세션이 진짜로 공유되고 오래 살아야만 나타나는 네 가지 속성을 측정한다: 동시 턴이 도는 *도중의* 공유 컨텍스트 낙관적 압축(4/4 커밋, 무락 요약 구간 안에서 11개의 첫 토큰을 포함한 42개 턴 이벤트 처리), 동시성 하의 결정적 응답 귀속(100/100 전단사, 그리고 구조적 귀속이 의도적으로 풀지 않는 *의미적* 혼동을 최악 38%로 정량화), 폭주 하의 사용자별 공정성(짧은 턴 대기 24× 감소), 수명주기 내구성(3회 중단/재개를 가로지른 204턴, 압축으로 유계된 컨텍스트). 실 온프레미스 모델이 순위를 확인하고 병렬성의 값을 N× 가 아니라 입력 토큰 1.49× 로 매긴다. 우리는 세션 공유가 *만들어내는* 두 문제, 즉 비대칭 그라운딩과 다자 제어권 중재를 분석하며 마무리하고, 이것이 멀티플레이어 인간-에이전트 프로그래밍의 연구 의제를 규정한다고 주장한다.

---

## 1 서론

코딩 에이전트는 임계점을 넘었다. 대규모 언어 모델(large language model, LLM) 위에 지어진 시스템들은 더 이상 자동완성을 제안하는 데 그치지 않는다. 저장소를 읽고, 파일을 편집하고, 셸을 실행하고, 테스트를 돌리고, 제한된 감독 하에 풀 리퀘스트를 연다 [1, 2]. 통제 연구는 에이전트가 사용자 혼자서는 끝내지 못했을 작업을 완수함을 발견했고 [2], 대규모 텔레메트리는 실제 세션의 41%에서 에이전트가 코드 전체를 작성했음을 보인다 [3]. 그러나 이 문헌 전체에서, 그리고 §2 에서 조사하는 배포된 모든 제품에서, 하나의 전제가 너무 일관되게 성립해 명시조차 되지 않는다: **한 인간, 한 에이전트.** 현대 코딩 에이전트의 상호작용 모델은 싱글플레이어다.

그러나 소프트웨어는 개인이 만들지 않는다. 이 불일치는 이제 업계에서 가시적이다. GitHub Next 의 Ace 프로토타입은 "2026년 초 현재, 모든 코딩 에이전트는 싱글플레이어 경험으로 설계돼 있다 — 그러나 소프트웨어를 만드는 일은 싱글플레이어 게임이 아니다"라는 선언으로 시작하며, 낭비된 작업·조정 부채·어긋난 산출물을 정확히 이 간극 탓으로 돌린다 [4]. Claude Code 사용자들은 구글 독스 스타일의 공유 세션을 요청해 왔다 [5]. 수요가 연구를 앞선다: 여러 인간이 하나의 자율 코딩 에이전트 세션을 동기적으로 공유하는 것에 관한 발표된 연구를 우리는 알지 못하며(§2.3), 그런 공유를 어떻게 조정해야 하는지 서술한 시스템 논문도 없다.

**메신저 경로와 그 천장.** 오늘날 *팀*이 코딩 에이전트를 쓰는 유일한 널리 배포된 방법은 메신저 통합이다. Claude Tag, OpenAI Codex, GitHub Copilot, Devin, Cursor 모두 Slack(일부는 Teams) 통합을 출하하며, 서로 독립된 벤더임에도 하나의 아키텍처로 수렴한다(§2.2): @멘션이 에이전트를 트리거하고, 스레드 이력이 컨텍스트로 포착되며, 작업은 복제된 저장소에 대해 분리된 클라우드 샌드박스에서 실행되고, 결과는 스레드에 게시되는 풀 리퀘스트(pull request, PR) 링크로 돌아온다. 이 설계에는 실질적 미덕이 있다: 팀이 대화하는 곳에서 만나고, 현대 구현은 테스트 실패에 맞서 자율적으로 반복한다. 그러나 그 구조가 부과하는 세 가지 한계는 어떤 반복으로도 제거되지 않는다. 첫째, *에이전트가 도는 동안 인간이 개입할 지점이 없다*: 스레드는 트리거이자 알림 표면이지 작업 공간이 아니므로, 조타는 실행 전이나 PR 후에만 일어난다. 둘째, *동시 지시를 위한 조정 프로토콜이 없다*: Anthropic 이 공유 채널에서 "누구든 조타하거나 남이 멈춘 곳에서 이어받을 수 있다"고 광고할 때 [6], 문서는 두 사람이 동시에 조타하면 어떻게 되는지 침묵한다. 셋째, *컨텍스트가 스레드 경계에 갇힌다*: 코드베이스의 암묵적 상태, 다른 채널에서 내려진 결정, 에이전트 자신의 이전 작업은, 흡수된다 해도 텍스트로 재흡수된다. GitHub 의 문서는 한 걸음 더 나아간 결과에 대해 이례적으로 솔직하다: 스레드 전체가 포착돼 결과 풀 리퀘스트에 저장되며, 벤더 스스로 이를 정보 노출 우려로 표시한다 [7].

**명백한 대안과 그에 반하는 통념.** 멘션-위임이 너무 분리돼 있다면, 팀이 그냥 하나의 라이브 에이전트 세션을, 하나의 대화 컨텍스트를, 하나의 작업 디렉토리를 공유하고 모두가 붙어 있으면 안 되는가? 통용되는 엔지니어링 통념이 공유 컨텍스트는 두 악 사이의 선택을 강요한다고 보기 때문이다. 하나는 *추론을 직렬화*하는 것이다: 바쁨 게이트가 스레드당 하나의 활성 실행만 허용하는데, LangGraph 의 이중 전송 전략(거부, 큐잉, 인터럽트, 롤백; 동시 실행은 그중에 없다) [8] 과 배포된 CopilotKit OpenTag 프레임워크(스레드당 분산 락을 잡고 동시 요청에 HTTP 409 로 답한다) [9] 가 그렇다. 이것은 선행 차단(head-of-line, HOL)을 재도입해, 늦게 온 사람의 한 줄짜리 질문이 남의 긴 빌드 뒤에서 기다리게 한다. 다른 하나는 *세션을 포크*하는 것이다: 사용자별 브랜치, 작업별 워크트리, 사후 수리를 동반한 낙관적 동시성 [10, 11, 12]. 이는 단일 공유 세션을 포기하고 그 대가를 병합 충돌과 컨텍스트 분할로 치른다: 당신의 에이전트는 내 에이전트가 방금 한 일을 본 적이 없다. 다중 에이전트 조정의 선행 연구는 비관적 직렬화가 추론을 막는다는 이유로 락을 쓰지 말라고 명시적으로 가르친다 [11].

**본 논문.** 우리는 그 선택이 거짓임을 보인다. 상충하는 것처럼 보이는 두 재화, 병렬 응답성과 공유 상태 무결성은 서로 다른 계층에 살고, 둘을 분리해 다루는 계약은 둘 다 가질 수 있다. **병렬 추론, 직렬 부수효과** 하에서 모든 사용자 메시지는 독립적인 *턴*이 되어, 다른 턴이 몇 개 비행 중이든 상관없이 즉시 LLM 추론을 시작한다; 여러 응답이 모든 참여자에게 동시에 스트리밍되고, 각각은 그것을 야기한 메시지에 1:1 로 귀속된다. 한편 *모든* 부수효과(파일 쓰기, 셸 실행, 패키지 설치, 공유 컨텍스트에 대한 모든 커밋)는 워크스페이스 단위 직렬 실행기를 통과하며, 이는 다시 충돌 단위 계층 락으로 정밀화되어 서로 다른 파일에 대한 쓰기는 병렬로 진행되고 셸 명령과 삭제는 배타로 남는다. 읽기 전용이고 지연시키기에 비싼 추론은 결코 막히지 않고, 순서 매기기가 싸고 인터리브되면 파국적인 효과는 결코 동시에 일어나지 않는다. 이 계약은 표준 교리에 의도적으로 반한다: 락을 걸되 추론 경로에서는 결코 걸지 않으며, 충돌을 수리하는 대신 예방한다. 병합도, 롤백도, 보상도 없다.

**증거는 어디서 오는가.** 우리는 이 계약을 목적에 맞춰 새로 지은 프로토타입에서 평가하지 않는다. **Coagora** 는 이 작업 이전에 이미 다중 동시 뷰어에게 *공유* 세션을 서비스하던 오픈소스 코딩 에이전트다: 하나의 에이전트 프로세스, 하나의 대화 컨텍스트, 하나의 작업 디렉토리, 근거리 통신망(local-area network, LAN) 웹 표면으로 붙은 N 개의 브라우저, 그리고 모두가 입력할 수 있는 구조. 없던 것은 *동시성*이었다. 공유 입력 큐가 한 번에 하나의 활성 실행만 허용했는데, 그것이 정확히 설계 공간의 직렬 계약이며 오늘도 이 시스템의 기본값이다. 이 사실은 백지 프로토타입이 흉내낼 수 없는 방식으로 평가에 중요하다: 우리의 기준선은 남이 하는 일을 약화시켜 재구성한 것이 아니라, 같은 전송·같은 컨텍스트 관리자·같은 도구·같은 계측을 쓰는, 시험 대상 시스템 자신의 프로덕션 경로다. 병렬 계약은 그 옆에 opt-in 게이트로 추가됐고, 배포된 다중 사용자 프레임워크들의 관행인 거부 계약 [8, 9] 은 같은 코드 위의 두 번째 게이트로 추가됐다. 그래서 §6 은 **계약만** 다른 세 조건을 비교한다.

**기여.**

1. **설계 공간**: 다중 사용자 코딩 에이전트 상호작용을 네 축(상태 소재, 동시성 계약, 귀속 단위, 개입 지점)으로 정리해 배포·프로토타입 시스템 아홉 개를 배치하고 빈 좌표를 드러낸다(§3).
2. **동시성 계약**: 병렬 추론과 직렬 부수효과를, 단일 공유 컨텍스트 위의 스냅샷 읽기/원자적 커밋, 턴 스코프 인터럽트를 동반한 턴 id 다중화, 충돌 단위 계층 부수효과 락, 사용자별 공정 입장으로 정식화한다(§4).
3. **작동하는 공유 세션 에이전트 내부의 구현**(§5): 락 스코프·동시성 계약·공정성 게이트를 각각 스위치로 노출해, §6 의 모든 주장이 **같은 바이너리 위에서** 도는 절제 팔을 갖게 했다. 결정적·키 불필요 재현 하네스와 커밋된 원시 데이터를 함께 제공한다.
4. **배포된 세 계약 전부에 대한 시스템 내부 실증 비교**(§6): TTFT 독립성(병렬 기울기 0.00 대 직렬 1.03, 거부-재시도 1.03, n = 240 ✔), 무결성 절제(8.2% → 0% ✔), 병렬 이득이 붕괴하는 지점과 락 입도가 되찾는 것의 측정(✔), 그리고 진짜로 공유되고 오래 사는 세션만이 보이는 다섯 속성: 늦은 합류자 증분 재생(§6.5), 동시 컨텍스트 압축(§6.6), 대규모에서의 구조적 결정적 귀속(§6.7), 폭주 하의 사용자별 공정성(§6.8), 중단·재개를 가로지른 수명주기 내구성(§6.9), 그리고 1.49×(N× 아님) 토큰 프리미엄이 측정된 실모델 확인(§6.10).
5. **연구 의제**: 세션 공유는 위임의 문제를 없애지만 자신의 문제를 만든다. 참여자 간 비대칭 그라운딩과 다자 제어권 중재이며, 이를 컴퓨터 지원 협업(Computer-Supported Cooperative Work, CSCW) 이론에서 도출해 열린 문제로 제기한다(§7).

---

## 2 관련 연구

### 2.1 한 개발자, 한 에이전트

AI 페어 프로그래밍 연구는 일관되게 2인조를 구현한다. Imai 의 초기 비교는 Copilot 이 생산 줄 수를 늘렸지만 인간 짝에 비해 품질을 떨어뜨렸음을 발견했고 [13], 이후 연구는 개발자가 AI 제안을 인간 파트너의 것보다 덜 검증함을 보였다 [1]. Chen 등은 어시스턴트와 자율 에이전트의 첫 통제 비교를 수행하고 *에이전트 행동의 이해*를 핵심 채택 장벽으로 지목했다 [2]. SWE-chat 은 실사용 에이전트 세션 6,000건을 분석해, 사용자가 기회의 39%에서 에이전트를 중단하거나 방향을 바꾸는 반면 에이전트는 거의 멈춰 묻지 않음을 발견했다 [3]. 이 발견들은 우리 동기를 두 겹으로 뒷받침한다: 중단율은 조타가 실제 에이전트 사용의 필수 요소임을 보여주고, 이 연구들 전부가 정확히 한 명의 인간이 브레이크를 쥐고 있다고 전제한다. 셋이면 누가 밟는가(§7.2)?

### 2.2 메신저를 통한 팀 접근

우리는 Claude Tag [14, 6], OpenAI Codex [15, 16], GitHub Copilot [17, 7], Devin [18], Cursor [19] 의 Slack/Teams 통합을 조사했다. 다섯 모두 같은 파이프라인(멘션 → 스레드 흡수 → 클라우드 샌드박스 → PR 링크)을 구현하며, 주로 정체성에서 갈린다: Claude Tag 는 에이전트에게 *자신의* 계정을 준다(자체 GitHub App 이 PR 을 열고, 작업은 조직에 귀속된다). Copilot 은 *요청한 사용자로서* 그 사용자의 저장소 권한 하에 행동하고, Devin 은 Slack 정체성을 이메일로 사용자별 계정에 매핑한다. 벤더의 두 가지 자인이 여기서 중요하다. GitHub 은 스레드 전체가 PR 에 포착됨을 문서화하고 그것이 허용되지 않을 때는 다이렉트 메시지를 권한다 [7]: 컨텍스트로서의 스레드는 노출 표면이며, 이 위험은 Slack AI 에 대한 간접 프롬프트 주입으로 실제 실현됐다(MITRE ATLAS 사례 AML.CS0035) [20]. 그리고 Claude Tag 의 채널당 인스턴스 하나 "멀티플레이어" 공유는 충돌하는 동시 지시에 대한 어떤 발표된 프로토콜도 없이 출하됐다 [6]. 우리는 이 계열을 허수아비가 아니라 정확히 특징짓는다: 현대 백그라운드 에이전트는 *실제로* 자율 반복하고(테스트 실패에 재시도), Devin 은 *실제로* 웹 앱과 Slack 을 가로질러 세션을 유지한다. 이 계열에 불변으로 없는 것은 실행 중의 인간 개입 지점과 다중 사용자 조정 메커니즘이다.

### 2.3 여러 인간과 하나의 AI: 코딩 밖에서, 또는 세션 밖에서

가장 가까운 연구 이웃들은 세 방향에서 접근하지만 어느 쪽도 우리 설정에 닿지 않는다. *협업 글쓰기*에서 Lehmann 등은 다중 사용자 편집기에 에이전트를 심고, 일주일간의 팀 사용에서 팀이 에이전트를 기존 협업 규범에 흡수했으며 에이전트 프로필은 개인 작업공간처럼, 산출물은 공유 자산처럼 기능했음을 발견했다 [21]. 그러나 문서에는 코딩 조정을 어렵게 만드는 실행 가능 상태·비가역 부수효과·객관적 테스트 신호가 없다. *그룹 대화*에서 Koala 는 다자 채팅에서 에이전트가 언제 발언권을 가져야 하는지 탐구했고 [22], GroupMemBench 는 에이전트가 다자 채널에 배치되는데도 에이전트 기억 시스템이 단일 사용자로 지어짐을 관찰한다 [23]. *교육*에서 Daryanto 등은 우리와 구성상 가장 가까운 연구를 수행했다: 참가자 20명이 2인조(인간 1 + AI)와 3인조(인간 2 + AI) 프로그래밍을 비교했고, 3인조가 AI 코드에 대한 과의존을 줄였음을 발견했다. 동료의 가시성이 참가자로 하여금 제안을 적용하기 전에 이해할 책임을 느끼게 했기 때문이다 [24]. 우리는 다섯 축에서 다르다: 그들의 설정은 협업 학습이고, 그들의 AI 는 파일·셸 효과를 가진 자율 에이전트가 아니라 제안 기반 어시스턴트이며, 과제는 일회성 실험실 연습이고, 종속 변수는 학습 성과이며, 지속되는 공유 세션 상태가 존재하지 않는다. 그럼에도 그들의 책임성 발견은 §7.3 에서 돌아올 가설을 제공한다. 마지막으로, 하나의 에이전트 *산출물* 주변의 여러 인간을 다루는 실증 문헌이 늘고 있다: 에이전트가 작성한 풀 리퀘스트에 대한 리뷰어 참여도가 통합을 강하게 예측한다 [25]. 그러나 이는 산출물에 대한 비동기 사후 협업이고, 라이브 세션은 여전히 싱글플레이어다. 최근 입장 논문은 인간-에이전트 상호작용에 관한 공개 데이터가 전반적으로 희소하다고 주장하는데 [26], 다중 인간 세션 데이터는 아예 존재하지 않는다.

### 2.4 공유 상태 위 에이전트를 위한 동시성 제어

시스템이 공유 가변 상태 위에서 에이전트를 실제로 동시 실행할 때, 지배적 패러다임은 낙관적이다. CoAgent 는 쓰기를 일단 반영하고 순서가 어긋난 것을 사가(saga) 방식 되돌리기로 수리하며, 비관적 락은 추론을 막으므로 부적합하다고 명시적으로 가르친다 [11]. STORM 은 하나의 코드베이스 위 여러 에이전트에 낙관적 동시성 제어(버전 추적과 쓰기 시점 충돌 감지)를 적용한다 [27]. DeLM 은 단일 입장 게이트를 통해 공유 컨텍스트로 병렬 추론을 허용한다 [12]. 상용 병렬 에이전트 제품들은 브랜치나 워크트리로 격리하고 나중에 병합한다 [10]. LangGraph 의 이중 전송은 한 스레드의 동시 요청에 대해 거부·큐잉·인터럽트·롤백을 열거하지만 동시 실행은 없다 [8]. OpenTag 는 분산 락과 409 로 스레드당 활성 실행 하나를 강제한다 [9]. 우리 계약은 이들이 공유하는 교리를 뒤집는다: 우리는 추론이 결코 기다리지 않는 **효과 계층만** 잠그므로 비관적 직렬화가 병렬 추론과 양립하게 되고, 효과가 적용 *이전에* 정렬되므로 낙관적 계열의 충돌 감지·롤백 기계가 회피되는 것이 아니라 아예 불필요하다. 다중성의 방향도 주목하라: 격리-후-병합 계열은 한 인간을 위해 *에이전트*를 병렬화하지만, 우리는 한 에이전트 위에서 *인간*을 병렬화한다.

인접한 세 갈래가 이 좌표가 다른 방향에서도 비어 있음을 확인해 준다. *서빙* 측에서 Justitia 같은 공정성 스케줄러는 가상 시간 큐잉으로 작업 병렬 에이전트들에 GPU 시간을 배분한다 [28]: *작업이나 에이전트* 사이의 공정성이지 하나의 세션을 공유하는 인간들 사이의 공정성이 아니다. 우리가 아는 한 사용자별 입장 게이트(§4.5)는 턴 입도에서의 첫 사용자 수준 공정성 메커니즘이다. *컨텍스트* 측에서 병렬 압축 파이프라인은 블록을 동시에 펼쳐 긴 단일 에이전트 이력을 요약한다 [29]: 압축 자체가 병렬이지만, *동시 사용자 턴이 덧붙이는 도중에* 대화를 압축한 선행 연구는 없으며 그것이 §6.6 이 재는 문제다. *다중 사용자* 측에서 최근 연구는 주체 간 권한 계층과 프라이버시를 다루고 [30], ProACT 는 협업 사용자 간 조정 붕괴를 감지한다 [31]. 둘 다 다중 사용자 *정책*을 다루지 동시 실행을 다루지 않으며, 동시성 계약의 경쟁자가 아니라 보완재다.

### 2.5 이론적 렌즈

네 개의 CSCW 전통이 세션 공유의 의미를 틀 짓는다. *그라운딩* [32]: 협업자는 매체가 정하는 비용으로 공통 기반을 점진적으로 축적한다. 에이전트가 끼면 그라운딩은 삼자 문제가 되고, 지속되는 세션은 축적된 인간-에이전트 기반을 늦은 합류자에게는 없는 일급 자산으로 만든다(§7.1). *작업공간 인식* [33, 34]: 누가 어디서 무엇을 하는지 아는 것. 공유 편집기의 텔레포인터와 구조적으로 동형이되, 에이전트는 어떤 인간보다 더 많은 상태를 더 빨리 바꾼다. *접합 작업(articulation work)* [35]: 협업을 가능하게 하는 조정 노동으로, 자동화는 이를 제거하기보다 이동시키는 경향이 있다(§7.4). *혼합 주도 상호작용* [36]: Horvitz 의 원칙은 에이전트가 언제 행동하고 언제 물어야 하는지를 다스리지만 한 명의 인간을 전제한다. 다자 설정은 *누구에게 물을 것인가*라는 차원을 더하는데, 원래 원칙은 이를 다루지 않는다.

---

## 3 다중 사용자 코딩 에이전트 상호작용의 설계 공간

§2.2 부터 §2.4 까지의 시스템들은 이질적으로 보이지만 네 개의 이산 축을 따라 변한다.

**D1. 상태 소재.** 에이전트의 세션 상태는 인간이 대화하는 공간에 대해 어디에 사는가? *바깥*(대화가 분리된 실행기를 트리거하고, 상태는 실행 시점에 뜬 사본)인가, *안*(대화 표면이 라이브 세션을 보는 창이고, 상태가 원본)인가.

**D2. 동시성 계약.** 에이전트가 바쁜 동안 두 번째 지시가 도착하면 어떻게 되는가? *거부*(바쁨 게이트, 409), *직렬화*(선입선출(FIFO) 단일 활성 턴), *배치*(입력들을 하나의 프롬프트로 묶어 융합된 답 하나), *격리-후-병합*(사용자/작업별 포크 후 나중에 조정), 또는 *병렬 + 직렬 부수효과*(본 논문).

**D3. 귀속 단위.** 산출물을 그것을 야기한 인간까지 어느 입도로 추적할 수 있는가? *풀 리퀘스트*, *작업*, 또는 *턴*(모든 에이전트 메시지가 하나의 사용자 메시지에 1:1 로 연결).

**D4. 개입 지점.** 인간은 언제 작업에 영향을 줄 수 있는가? *실행 전/완료 후*뿐인가, *실행 중*인가, 실행 중이라면 세션 전체 범위인가 개별 턴 범위인가?

표 1 이 아홉 시스템을 배치한다. [NOTE: 제출 시 제대로 된 표/도표로 렌더.]

| 시스템 | D1 소재 | D2 계약 | D3 귀속 | D4 개입 |
|---|---|---|---|---|
| Claude Tag [6] | 바깥 | 미정의 (공유 인스턴스, 프로토콜 없음) | 작업 → 조직 정체성 | 전/후 |
| Codex Slack [15] | 바깥 | 멘션마다 새 클라우드 작업 | 작업 | 전/후 |
| Copilot Slack [17] | 바깥 | 채널당 직렬화 기본 | PR → 요청자 정체성 | 전/후 |
| Devin Slack [18] | 바깥 (동기화) | 세션 재개; 세션 내 직렬 | 작업 → 매핑된 사용자 | 스레드 내 답글 (거친 입도) |
| Cursor Slack [19] | 바깥 | 호출마다 격리된 가상 머신(VM) | 작업 | 전/후 |
| OpenTag [9] | 안 (스레드) | **거부** (락 + 409) | 메시지 | 실행 중 불가 |
| 병렬 에이전트 도구 [10] | 바깥 (브랜치별) | 격리-후-병합 | 브랜치 | 에이전트별 |
| Coagora, 직렬 모드 (이 시스템이 원래 출하하던 모드; 지금도 기본값) | **안** | **직렬화** (공유 입력 큐; 선착순 응답 게이트) | 턴 (작성자 라벨) | 실행 중, 세션 범위 |
| **Coagora, 병렬 계약 (본 논문)** | **안** | **병렬 + 직렬 부수효과** | **턴 (`reply_to` 1:1)** | **실행 중, 턴 범위** |

세 가지 관찰. 첫째, 메신저 계열은 하나의 조밀한 군집(바깥 / 작업 / 전-후)을 차지한다: §1 에서 말한 한계들은 어느 벤더의 실행이 아니라 *좌표*의 속성이다. 둘째, 오른쪽 아래 좌표(상태 안, 병렬 계약, 턴 귀속, 턴 범위 개입)는 비어 있었다. 셋째, 두 Coagora 행은 정확히 한 축 D2 에서만 다르며, 그것이 §6 을 통제된 비교로 만든다: 같은 시스템, 같은 전송, 같은 컨텍스트 관리자에서 계약만 바꾼 것이다. 본 논문의 나머지는 끝에서 두 번째 행에서 마지막 행으로의 이동에 대한 설계·구현·측정이고, 설계 공간은 비교 조건도 지목한다: *직렬화*와 *거부*는 허수아비가 아니라 배포된 시스템들이 실제로 쓰는 두 계약이며 그중 하나는 이 시스템 자신이 출하하던 것이므로, §6 은 셋 모두를 잰다.

---

## 4 계약: 병렬 추론, 직렬 부수효과

### 4.1 모델

**세션**은 삼중항 *S = (C, W, T)* 다: 단일 공유 대화 컨텍스트 *C*(순서 있는 메시지 목록), 단일 작업 디렉토리 *W*(하나의 경로에 뿌리내린 격리 워크스페이스), 그리고 턴 집합 *T*. 서로 독립인 임의 개수의 클라이언트가 하나의 세션에 붙고, 에이전트의 출력은 한 번 생성돼 모든 구독자에게 순서 있는 이벤트 스트림으로 팬아웃된다. 세션당 에이전트 프로세스는 하나이며, 접속이 사용자별 프로세스·VM·컨텍스트 포크를 만드는 일은 결코 없다.

계약은 두 개의 약속이다:

> **(P) 병렬 추론.** 모든 사용자 메시지는 독립적인 턴 *t_i* 가 된다. *t_i* 의 추론은 다른 비행 중 턴과 무관하게 즉시 시작하고, 응답은 모든 참여자에게 동시에 스트리밍된다.
>
> **(S) 직렬 부수효과.** 어떤 턴이 만드는 모든 효과(파일 쓰기, 도구 실행, *C* 에 대한 모든 커밋)는 워크스페이스별 직렬 입장 메커니즘을 통해, 완료 순서로, 충돌하는 효과와 결코 동시가 아니게 적용된다.

이 비대칭에는 원칙이 있다. 추론은 읽기 전용이고 길며(초에서 분) 사용자가 기다리는 대상이다. 그것을 막으면 한 사용자의 작업 길이가 다른 사용자의 지연으로 변환된다. 효과는 짧고, 순서 매기기가 싸며, 턴들이 서로를 오염시킬 수 있는 유일한 통로다. 두 번째 계층을 직렬화하는 것은 비용이 적고 실패 부류 전체의 부재를 산다. 첫 번째를 직렬화하는 것은 사용자들이 찾아온 이유 전부를 잃는다.

### 4.2 턴: 독립성, 다중화, 1:1 귀속

동시 메시지는 결코 하나의 프롬프트로 배치되지 않는다. 배치(D2)는 누구의 것도 아닌 융합된 답을 만들고, 메시지별 추론 파라미터를 파괴하며, 턴별 중단 창을 없앤다. 우리는 귀속을 UI 의 사치가 아니라 정확성 속성으로 취급한다. 각 턴은 *디스패치* 시점에(큐 진입 시점이 아니라. 식별자가 항상 실제로 모델에 발행된 작업에 대응하게 하려는 것이다) 단조 증가하는 **턴 id** 를 받는다. 스트리밍되는 모든 청크, 도구 호출, 도구 확인, 오류는 자기 턴 id 로 태깅되고, 런타임은 에이전트의 단일 출력 스트림을 턴별 싱크로 역다중화한다. 영속 저장소에서 각 에이전트 응답은 그것을 야기한 인간 메시지에 대한 `reply_to` 외래 키를 지닌다. 사용자 인터페이스는 동시 스트림을 각각 배지가 붙은 카드로 렌더한다.

**턴 범위 인터럽트.** 인터럽트는 턴 id 를 지목해 그 턴만 취소한다: 플래그가 서고, 부분 출력은 마감돼 보존되며(폐기가 아니라 완료 표시), 슬롯이 회수되고, 다른 비행 중 턴의 추론·스트리밍·효과는 손대지 않는다. 소유권이 강제된다: 인터럽트 요청은 호출자의 연결 id 를 실을 수 있고, 그러면 레지스트리는 호출자가 소유하지 않은 턴의 취소를 거부한다. 한 사용자가 다른 사용자의 작업을 멈출 수 없다는 뜻이다. 대칭적으로, 인터페이스는 각 사용자를 *자기 자신의* 활성 턴에 대해서만 게이팅한다: 남의 실행 중 턴이 당신의 입력창을 잠그는 일은 없다. 이것은 다자 제어에 대한 가장 작은 유용한 답이며(각 사용자가 자기 작업을 지휘한다), §7.2 는 이것이 왜 최종 답이 아닌지 논한다.

### 4.3 공유 컨텍스트: 스냅샷 읽기, 완료 순서 원자 커밋

턴들은 하나의 대화 컨텍스트 *C* 를 공유한다. 이것이 시스템의 요점이며, *C* 를 사용자별로 포크하는 것은 격리-후-병합을 조용히 재도입하는 일이다. 두 규칙이 동시성 하에서 *C* 를 정합하게 유지한다:

- **스냅샷 읽기.** 각 추론 스텝에서 턴은 *C* 의 불변 스냅샷을 프롬프트로 읽는다. 다른 턴의 비행 중 부분 출력은 결코 보이지 않는다.
- **완료 순서 원자 커밋.** 스텝이 끝나면 턴은 결과(어시스턴트 메시지와 *모든* 대응 도구 결과 메시지)를 하나의 원자 블록으로, 직렬 커밋 사슬을 통해, 완료 순서로 커밋한다. 블록 경계는 불변식이다: 어시스턴트/도구-결과 쌍을 쪼개면 그 사이로 다른 턴의 커밋이 끼어들 수 있고, 이는 LLM 채팅 API 가 강제하는 메시지 짝 제약을 위반해 요청 자체가 거절된다.

알려진 비용은 유계 staleness 다: 턴의 스냅샷은 그 스냅샷 이후 다른 턴이 커밋한 효과를 빠뜨릴 수 있다. 우리는 이를 풀기보다 공개한다(§7.5). 중요한 속성은 staleness 가 *신선도*의 한계일 뿐 결코 *일관성* 위반이 아니라는 것이다. *C* 는 언제나 어떤 직렬 이력이 만들어낼 수 있었을 잘 형성된 트랜스크립트이기 때문이다.

### 4.4 효과 계층: 전역 뮤텍스에서 충돌 단위 계층 락으로

첫 효과 실행기는 모든 효과를 워크스페이스당 하나의 뮤텍스로 직렬화했다. 정확했고, 너무 뭉툭했다: 같은 경로를 결코 건드리지 않을 턴들도 서로 뒤에 줄을 섰다. §6.4 는 그 비용을 가정하지 않고 이 시스템에서 측정하며, 그 측정은 언제 그 비용을 걱정해야 하는지도 말해 준다. *서로소* 파일에 쓰는 두 턴은, 효과가 각 턴 구간의 큰 비중을 차지하게 되면, 전역 뮤텍스 하에서 유효 병렬도 1.37 대 충돌 단위 하 1.98 로 1.45× 를 되찾는다. 효과 비중이 낮으면 두 스코프는 구별되지 않는데, 전역 뮤텍스는 효과가 실제로 차지하는 만큼만 비용을 낼 수 있기 때문이다.

그래서 v2 실행기는 분류된 효과 의도(effect intent) 위의 호환성 행렬을 써서 *충돌* 입도로 직렬화한다:

| 의도 쌍 | 결정 | 근거 |
|---|---|---|
| FILE_WRITE/READ(경로 *p*) 대 FILE_WRITE/READ(경로 *q*), *p* ≠ *q* | **병렬** | 서로소 자원 |
| FILE_WRITE/READ(*p*) 대 FILE_WRITE/READ(*p*) | 직렬 | 같은 자원 |
| 무엇이든 대 SHELL 또는 PACKAGE | 직렬 (배타) | 셸의 파일 발자국은 정적으로 알 수 없다(파이프, 전개, 서브셸). 건전한 경로 키가 없다 |
| 무엇이든 대 FILE_DELETE | 직렬 (배타) | 삭제는 디렉토리를 지울 수 있다. `rm -r src/` 를 `write src/x.py` 옆에서 돌리면 배타 하에서는 없던 ENOENT 경쟁 부류가 *생긴다*. 삭제는 드물어 배타성이 거의 공짜다 |
| 정렬 대상 아님 (복합 도구, 사람 대기 도구, 워크스페이스 밖 상태) | **락 없음** | 이들에게는 이 락이 정렬할 워크스페이스 효과가 없다. 아래 참조 |
| 알 수 없는 파일 효과 | 직렬 (배타) | 안전 우선 기본값 |

경로 키는 정규화된다(구분자 통일, 대소문자 구분 없는 파일시스템에서는 케이스 폴딩. `A.txt` 와 `a.txt` 는 반드시 충돌해야 하기 때문이다). 입장은 **워크스페이스당 엄격 FIFO, 추월 금지**다: 큐 머리가 실행 집합과 호환되지 않으면 펌프는 그것을 건너뛰어 훑는 대신 멈춘다. 이는 동시성을 다소 희생해 단단한 공정성 보장을 택한 것이다: 파일 연산의 흐름이 대기 중인 셸 명령을 굶길 수 없다. 락 스코프는 런타임 스위치(`--lock-scope off | workspace | conflict`)로, 롤백 경로이자 §6.2·§6.4 의 절제 팔 노릇을 함께 한다.

*정렬 대상 아님* 행은 이 시스템의 스레드 모델이 강제한 정밀화이며, 명시할 가치가 있다. "알 수 없는 의도 → 배타"라는 순진한 독법은 교착이기 때문이다. 복합 도구(서브에이전트, 스킬)는 중첩 에이전트 루프를 띄우고 그 잎 도구 호출들이 *다른* 스레드에서 같은 락을 요구한다. 부모가 배타 락을 쥔 채 자식이 요구하면 교착이고, 자식은 같은 스레드가 아니므로 재진입도 도움이 되지 않는다. "알 수 없음"에 실제로 무엇이 떨어지는지 살펴보니 한 속성을 공유하는 세 무리였다: 복합 도구(잎들이 각자 잠근다), 사람 대기 도구(사람의 답을 기다리는 동안 배타 락을 쥐면 사람이 걸리는 시간만큼 다른 모든 턴의 효과가 멈춘다), 그리고 상태가 워크스페이스 밖에 사는 도구(세션 메모리, 코드 인덱스, 네트워크 페치. 각자 자기 가드가 있다). 셋 다 이 락이 정렬할 워크스페이스 효과가 없으므로, "알 수 없음"의 운용 의미를 *이 락의 소관이 아님*으로 좁히고, 진짜로 알 수 없는 *파일* 효과에는 배타 기본값을 유지한다.

낙관적 계열(§2.4)과의 대비가 이제 정확해진다. "락은 추론을 막는다"는 CoAgent 의 가르침은 *추론 경로에 있는* 락에 대해 참이고 여기서는 무관하다: 추론은 이 락을 결코 획득하지 않는다. 그리고 호환되지 않는 효과가 적용 *이전에* 정렬되므로, 감지하거나 되돌리거나 보상하거나 병합할 것이 없다. 같은 자원에 대한 쓰기의 논리적 결과는 충돌 해소가 아니라 *순서의 구성에 의한* last-wins 다.

### 4.5 입장: 상한, 공정 큐, 사용자당 턴 하나

무한 병렬성은 사용자의 열의를 무한한 토큰 비용으로 바꾼다. 세션은 동시성 상한(기본 비행 중 턴 4개)을 강제한다. 상한을 넘으면 입력은 거부되지도 배치되지도 않고 공정 큐에 들어가며, 큐는 머리에서 꼬리로 훑어 첫 *적격* 항목을 찾는다. 적격이란 제출한 사용자에게 활성 턴이 없다는 뜻이다(**사용자당 활성 턴 하나**: 병렬성은 사용자 *사이에* 존재하지 한 사용자의 백로그 *안에* 존재하지 않으므로, 한 사용자가 다섯 요청을 큐에 넣어 슬롯을 독점할 수 없다). 시스템이 시작한 턴은 사용자별 게이트에서 면제된다. 큐에 든 항목이 다른 사용자의 즉시 디스패치를 지연시키는 일은 없다. §6.1 은 정중해 보이는 대안, 즉 거부하고 재시도하게 하는 쪽이 왜 직렬보다 나은 적이 없는지 보인다.

### 4.6 세션 상태와 수명주기

세션 상태는 *개수 기반*이다: 비행 중 턴이 하나라도 있으면 RUNNING, 0이면 IDLE. 완료가 유휴를 정의하는 "그" 활성 턴 하나 같은 것은 없고, IDLE 로의 전이는 마지막 턴에 게이팅되어 세션 중간의 깜빡임을 피한다. 활동이 0이면 세션은 중단될 수 있고(프로세스 종료, 디렉토리와 파일 보존) 나중에 durable 이력에서 재개된다. 병렬 경로는 **세션 시작 시 고정되는 opt-in 게이트**이며 프로세스 수명 동안 불변이다. 그래서 직렬 턴과 병렬 턴이 한 세션에 공존하는 일이 없고 컨텍스트 정합 규칙이 수명 중간에 재협상되지 않는다. 인식할 수 없는 계약 이름은 직렬로 조용히 떨어지는 대신 기동 오류다: 계약을 조용히 강등시키는 오타 하나가 측정 실행 전체를 무효로 만들기 때문이다.

### 4.7 순서, 재생, 멱등성

공유 세션은 들락거리는 클라이언트들이 지켜보므로, 이벤트 스트림에는 끊긴 연결에서 살아남는 순서가 필요하다. 모든 영속 이벤트는 단조 증가하는 시퀀스 번호 `seq` 를 지니며, 이는 이벤트가 재생 버퍼에 들어가는 단일 지점에서, 그것을 덧붙이는 것과 같은 락 아래 발급된다. 발급 지점이 중요하다: 팬아웃에서 `seq` 를 발급하면 동시 턴 두 개가 한 순서로 번호를 받고 다른 순서로 덧붙일 수 있어, 재접속하는 클라이언트가 어떤 라이브 뷰어도 본 적 없는 열을 재생하게 된다.

`seq` 는 재접속 재생의 좌표다. 와이어에서는 서버 전송 이벤트(Server-Sent Events, SSE)의 `id` 필드로 이동하며, 브라우저는 자동 재접속 시 이를 `Last-Event-ID` 로 되돌려주므로, 떨어진 클라이언트는 트랜스크립트 전체가 아니라 *놓친 것만* 받는다. 증분으로 서비스할 수 없는 세 경우는 대신 리셋 신호와 전체 스냅샷으로 답한다: 재생 버퍼의 보존 창보다 오래된 커서, 발급한 적 없는 앞선 커서, 그리고 이전 프로세스가 발급한 커서. 마지막 경우는 모든 id 에 프로세스별 스트림 epoch 를 태깅해 처리하며, 이는 재개 후 재기동을 조용한 접합이 아니라 리셋으로 만든다. 새 세션의 이벤트가 옛 트랜스크립트에 이어 붙는 일을 막는 것이다. *영속* 이벤트만 `seq` 를 지니고 transient(토큰 델타, 스피너)는 지니지 않는데, 이는 클라이언트의 커서를 버퍼가 실제로 재현할 수 있는 마지막 이벤트에 고정한다. §6.5 는 반복해서 끊긴 클라이언트가 한 번도 끊기지 않은 클라이언트와 같은 트랜스크립트에 도달하는지 잰다.

---

## 5 구현

Coagora 는 약 38,400줄의 Python 과 약 6,300줄의 의존성 없는 브라우저 자산으로 이루어진 오픈소스 코딩 에이전트다. 한 사용자를 위한 터미널 프로그램으로도, 여럿을 위한 LAN 웹 서버로도 돈다. 두 프런트엔드 모두 렌더러 인터페이스를 통해 같은 에이전트 루프를 구동하며, 그래서 다중 사용자 작업이 두 번째 실행 경로를 요구하지 않았다. 여기 보고하는 모든 것은 원시 데이터와 함께 공개 리포지토리에 있다. 이 절은 재현자가 필요로 할 결정들, 특히 세션이 동시성을 획득했을 때 비로소 필요해진 것들을 서술한다.

**한 프로세스, 여러 스레드.** 세션은 하나의 운영체제 프로세스다. 턴은 메시지를 주고받는 자식 프로세스가 아니라 공유 힙 위의 스레드다. 이것이 뒤따르는 대부분이 파생되는 아키텍처적 사실이다: 효과를 중개할 부모가 없으므로 §4 의 보장은 하나의 주소 공간 안에서 규율로 강제되어야 한다.

**컨텍스트 관리자의 락 규율.** 공유 컨텍스트는 시스템에서 가장 경합이 심한 객체로, 모든 턴의 스냅샷 읽기와 원자 커밋이, 그리고 압축(§6.6)도 건드린다. 그 락들은 컨텍스트 → 렌더러 → 파일 append 한 방향으로만 획득되며 역방향은 결코 없다. 이 순서는 각 획득 지점에 문서화돼 있는데, 여기서 순환이 생기면 턴 하나가 아니라 세션 전체가 교착되기 때문이다.

**귀속은 스레드 로컬이고, 이것은 어렵게 배웠다.** 턴이 답하고 있는 질문의 정체성은 스레드 로컬 상태에 살고, 그래서 그 질문을 처리하는 동안 쓰이는 모든 레코드는 모델이 뭐라 말하든 자기 `reply_to` 를 지닌다. 첫 구현은 이를 세션 전역 필드에 두었다. 직렬 계약에서는 한 번에 턴이 하나뿐이므로 그 필드가 구성상 정확하다. 선점형 스레드 하에서는 동시 답변 세 개를 조용히 마지막 질문에 귀속시켰다. 이 버그는 라이브에서 잡혔고, 필드를 스레드 로컬 저장소로 옮겨 고쳤으며, 회귀 테스트로 고정했다. §6.7 이 이것이 이제 정확함을 재는 측정이다.

**Append 원자성은 파일시스템에 달렸다.** 쓰기를 직렬화할 부모 프로세스가 없으므로 durable 이력에는 모든 턴이 직접 덧붙인다. 탐침 결과 로컬 ext4 에서는 안전했지만(append 한 번이 `write(2)` 한 번이라 `O_APPEND` 원자성이 성립하고, 16 스레드가 8회씩 덧붙여도 무손상), WSL(Windows Subsystem for Linux)의 Windows 드라이브 마운트에서는 같은 탐침이 데이터 대부분을 잃었다: 4 KiB 페이로드에서 128줄 중 45줄만 남고 그중 15줄이 깨진 JSON 이었으며, 1 MiB 에서는 128줄 중 28줄이었다. 실패 양상이 위험한 종류인데, 레코드 로더가 파싱 불가한 줄을 조용히 건너뛰기 때문이다. 재개된 세션은 오류 없이 그냥 이력이 비어 있게 된다. 수리는 스트라이프된 프로세스 내 append 락이다(경로당 락 하나가 아니라 고정 개수의 스트라이프이므로 긴 세션이 락을 무한히 쌓지 않는다). 락을 걸면 같은 탐침이 128/128, 손상 0 으로 돌아온다. 하나의 공유 이력 파일에 대한 동시 append 는 다중 사용자 병렬 턴이 만들고 그 외에는 아무것도 만들지 않는데, 이것이 이 위험이 계약과 정확히 같은 시점에 나타난 이유다.

**도구 호출은 스트리밍된 content 에서 파싱한다.** 에이전트는 제공자 네이티브 함수 호출에 의존하지 않고 모델의 content 스트림에서 JSON 연산 배열을 파싱한다. 이는 함수 호출 지원이 제각각인 온프레미스 서버들을 가로질러 이식성을 유지한다. 파서는 증분식이라, 응답 스트리밍이 끝나기 전에 그 턴의 첫 도구 호출을 디스패치할 수 있다.

**웹 표면.** 다중 사용자 표면은 HTTP 다: 클라이언트당 하나의 SSE 스트림이 하향, 평범한 POST 가 상향. 여기서 SSE 는 WebSocket 대신 의도적으로 고른 것이다. 브라우저 네이티브 자동 재접속과 `Last-Event-ID` 가 §4.7 의 재생을 애플리케이션 기계가 아니라 전송의 속성으로 만들어 주고, 상향 방향은 가끔 오는 작은 메시지만 나르기 때문이다. 인증은 상수 시간으로 비교되는 공유 세션 토큰이다. 두 번째 읽기 전용 토큰을 켤 수 있으며(기본 off), 그것을 제시한 클라이언트는 스트림 구독만 할 수 있다: 모든 변이 엔드포인트가 403 으로 답하고, 트랜스크립트 이상을 노출하는 읽기 엔드포인트(프롬프트 검사, 프로젝트 지시사항, 워크스페이스 내용)도 마찬가지다. 이 분리가 존재하는 이유는 "보게 하기"와 "행동하게 하기"가 서로 다른 부여이고, 시연이나 리뷰는 전자이기 때문이다.

**중단과 재개.** 세션의 durable 상태는 append 전용 이력 파일이다. 재개 시 렌더러는 클라이언트가 붙기 전에 그 이력을 이벤트 버퍼로 재생하므로, 재접속하는 브라우저는 이전 대화를 특수 경로가 아니라 평범한 스냅샷 경로로 본다. 식별자 카운터는 압축된 캐시가 아니라 *전체* 이력에서 재도출되므로, 재개된 세션이 압축은 작업 창에서 떨어뜨렸으나 기록에는 귀속이 남아 있는 id 를 다시 발급하는 일이 없다(§6.9).

**계측 평면, opt-in.** `--turn-metrics` 를 주면 세션은 구조화된 이벤트를 `turns.jsonl` 에 덧붙인다: 턴 수명주기(큐 진입 → 디스패치 → 첫 토큰 → 완료), 효과 락의 큐 진입/획득/해제와 대기·보유 시간, 압축 세대, 거부, 콜별 토큰 사용량이 전부 하나의 단조 시계 위에 놓인다. §6 의 모든 수치는 이 파일이나 durable 이력에서 계산되고 클라이언트 측 타이밍은 결코 쓰지 않으므로, 시계 차 보정이 없고 거부 계약의 재시도 대기는 서버가 본 첫 409 부터 측정된다. 이 평면은 구조적 메타데이터만 기록하고 프롬프트나 응답 텍스트는 결코 기록하지 않는다.

**과학을 위한 결정성.** 벤치마크 하네스(§6, `bench/multiuser/`)는 실제 서버를 HTTP 로 구동해 로컬 목 LLM 을 향하게 한다: OpenAI 호환 SSE 서버로, 지연·토큰 수·도구 스텝이 프롬프트에 박힌 지시자(`[[bench ttft= tok= n= work= fwrite= …]]`)로 스크립트되며 난수나 시계 의존 분기가 없다. 같은 입력이 같은 타임라인을 만들고, 스위트는 네트워크나 자격증명 없이 돌며, 각 조건은 격리된 `HOME` 을 가진 새 임시 워크스페이스를 받으므로 벤치마크가 운영자의 설정을 읽지도 쓰지도 못한다. 두 실험은 모델 계층을 의도적으로 우회해 도구와 락을 직접 구동한다(§6.2, §6.4). 그 이유는 가정이 아니라 하네스의 **실측된 속성**이다. 목은 대화에서 진행도를 읽는데(가장 최근 지시자 이후의 관찰), *공유* 세션에서는 동시 턴들의 관찰이 하나의 계수기에 들어온다. 각 호출이 어느 지시자를 해소했는지 로깅하면 결과가 정확히 보인다: 각 턴의 첫 호출은 자기 지시자를 고르지만 연속 호출부터 가장 최신 것으로 붕괴하고, 그 뒤 한 턴이 다른 턴의 경로에 쓰다가 에이전트 자신의 반복 감지에 걸린다. 공유 프롬프트 안에 목이 키로 삼을 턴별 신호가 없으므로 이것은 고칠 버그가 아니라 하나의 공유 컨텍스트로 **서로 다른** 동시 워크로드를 스크립트하는 것의 한계다. 동시 턴이 **같은** 워크로드를 돌아도 되는 실험(§6.1, §6.6~§6.9)은 영향받지 않으며 목을 쓴다. 통제 스윕이 아니라 실제 작동점이 질문인 곳(§6.4)에서는 자기 턴의 요청을 읽어 이 한계가 없는 실모델을 쓴다.

---

## 6 평가

우리는 열 개의 연구 질문을 평가한다. **RQ1** 계약이 선행 차단을 제거해, 한 사용자의 지연이 다른 사용자의 작업과 독립이 되는가? **RQ2** 대안 배포 계약들은 무엇을 치르는가? **RQ3** 효과 직렬화가 실제로 오염을 막는가? **RQ4** 병렬 이득은 어디서 붕괴하고, 락 입도는 무엇을 되찾으며, 실제 작업은 그 곡선 위 어디에 앉는가? **RQ5** 연결이 계속 끊기는 클라이언트가 한 번도 끊기지 않은 클라이언트와 같은 세션에 도달하는가? **RQ6** 턴이 도는 동안 공유 컨텍스트를 압축할 수 있는가? **RQ7** 귀속이 휴리스틱이 아니라 구조적일 수 있는가? **RQ8** 사용자별 입장이 폭주 하에서 실제로 다른 사용자를 보호하는가? **RQ9** 세션이 재기동과 수백 턴을 견디는가? **RQ10** 순위가 실 LLM 에서도 유지되며, 병렬성은 토큰으로 실제 얼마를 치르는가?

**설정.** 모든 대표 수치는 §5 의 결정적 하네스를 쓴다: 스크립트된 작업 길이의 목 LLM, 실제 서버, 실제 워크스페이스, 실제 락, 실제 전송. 모델만 모사되므로 측정 구간은 시험 대상 시스템이 지배하고, 모든 실행은 자격증명 없이 리포지토리에서 재현 가능하다. 비교의 모든 팔은 스위치만 다른 같은 바이너리이므로(`--concurrency-contract`, `--lock-scope`, `--per-user-gate`), 팔 사이의 차이는 계약의 차이이지 그 주변 코드 경로의 차이가 아니다. 실 온프레미스 모델에 대한 스팟체크는 §6.10 에 있다. 모든 그림의 원시 JSONL 은 `bench/multiuser/out/` 에 커밋돼 있다. ✔

**고정 오버헤드에 관한 주의.** 모든 턴은 상수 셋업 비용(에이전트 루프 조립 + 스크립트된 첫 토큰 지연)을 진다. 계약 간에 동일하므로 기울기와 동일 조건 비교에서는 상쇄되지만, 측정 세션 사이에는 약간 이동한다. 아래의 선행 차단 그리드는 두 세션에서 수집됐고(L ∈ {2, 6, 15} s, 그리고 나중에 L = 30 s), 그 사이 상수가 약 56 ms 줄었다. 두 번째 세션에서 L = 15 s 를 대조 재측정하니 두 번째 세션의 상수를 정확히 재현하므로, 이 이동은 *L* 의 효과가 아니라 환경적임이 확인된다(`out/e2-drift-control.json` ✔). 결과는 유계이고 해당 위치에 명시한다: 기울기는 면역이다(상수 오프셋은 회귀에서 상쇄된다). 한 *L* 안의 계약 간 비교도 면역이다(각 세션이 그 레벨의 세 계약을 함께 측정했다). 레벨 간 절대값 비교만이 경계를 넘는다.

### 6.1 RQ1 과 RQ2: 세 계약 하의 선행 차단 ✔

프로토콜: 사용자 A 가 길이 *L* ∈ {2 s, 6 s, 15 s, 30 s} 인 작업을 시작하고, 0.5 s 뒤 사용자 B 가 한 줄짜리 질문을 보낸다. B 의 첫 토큰까지의 시간을 B 의 첫 제출 시도부터 측정한다(거부 팔은 첫 409 부터이므로 클라이언트의 재시도 대기가 그 팔에 계산된다). 계약: *직렬*(FIFO, 이 시스템이 출하하던 모드), *거부-후-재시도*(바쁨 게이트가 409 를 반환하고 클라이언트가 250 ms 마다 재시도하는 OpenTag/이중 전송 계열), *병렬*. 총 n = 240(셀당 20).

| 계약 | L = 2 s | L = 6 s | L = 15 s | L = 30 s | 기울기 dTTFT/dL |
|---|---|---|---|---|---|
| 직렬 (FIFO, 출하 모드) | 2.08 s | 6.16 s | 15.34 s | 30.86 s | 1.028 |
| 거부 + 재시도 | 2.31 s | 6.34 s | 15.42 s | 31.05 s | **1.027** |
| **병렬 (우리)** | **0.292 s** | **0.291 s** | **0.291 s** | **0.235 s** | **-0.002** |

중요한 수는 비율이 아니라 *기울기*다. 기울기 ≈1.03 에서는 당신 빌드의 1초가 곧 내 기다림의 1초다. 비율(L = 2 s 에서 7.1×, 6 s 에서 21.2×, 15 s 에서 52.7×, 30 s 에서 131×)은 효과가 끝나는 지점이 아니라 우리 측정이 멈추는 지점을 기록할 뿐이며, 그래서 우리는 기울기를 발견으로, 비율을 예시로 보고한다. 병렬 계약의 기울기 ≈0.00 은 B 의 지연이 B 자신의 요청의 속성이라는 뜻이고, 그것이 독립성의 정의다. 병렬의 p95 는 모든 셀에서 p50 보다 2 ms 위에 있다.

두 가지 독법에 주의가 필요하다. 첫째, 병렬 행은 한 측정 세션의 세 레벨에서 0.291 s 로, 다른 세션에서 측정된 레벨에서 0.235 s 로 평평하다. 통합 기울기는 -0.002 이고 세션 내 기울기는 -0.00003 이며, 둘은 같은 말을 한다. 눈에 보이는 계단은 설정 절에서 서술한 고정 오버헤드 드리프트이고, 그 자체가 주장의 작은 예증이다: 병렬 계약의 TTFT 는 *그 상수 외에 아무것도 아니므로* 상수의 변화가 거기서는 전부 드러나는 반면, *L* 이 지배하는 직렬·거부 행에서는 보이지 않는다(같은 레벨에서 15.337 s → 15.360 s, 0.15% 이동).

둘째, 거부 계약은 배포된 다중 사용자 프레임워크들이 실제로 출하하는 것이므로 강조할 가치가 있다. 이것은 직렬의 선행 차단 대기를 *전부* 치른 *뒤* 세금을 더 낸다. 250 ms 재시도 간격에 대해 네 레벨에서 각각 +232 ms, +182 ms, +79 ms, +190 ms 로 측정된, 간격 1개분의 위상 의존 분수다. 아래로는 재시도가 마침 실행 중 턴이 끝나는 순간에 떨어질 때 0 이고, 위로는 간격 하나가 상한이다. 따라서 *직렬보다 나은 적이 없으며*, 덤으로 재시도 기계를 모든 클라이언트에 떠넘긴다. 기울기가 직렬과 같은 이유는 그 세금이 *L* 의 함수가 아니라 상수이기 때문이다: 거부는 대기를 더 빨리 자라게 만드는 것이 아니라, 같은 대기를 조금 더 길게 만들고 클라이언트를 더 복잡하게 만든다.

### 6.2 RQ3: 효과 직렬화가 막는 것 ✔

절제: 두 동시 writer 가 구별 가능한 마커와 체크섬 본문으로 같은 파일에 반복해서 쓰고, 효과 락을 끈 경우와 켠 경우를 비교한다. 2 ms 샘플러가 스냅샷을 intact, mixed(두 writer 의 마커가 함께 존재하는 찢어진 쓰기), broken(구조나 체크섬 무효), partial(쓰기 진행 중. `open("w")` 가 정당하게 truncate 후 기록하므로 계약 위반이 아니다)로 분류한다.

끈 경우(`--lock-scope off`), 분류된 스냅샷의 **8.2%** 가 위반이었다(110개 중 9개, 전부 두 writer 의 내용이 섞인 찢어진 쓰기). 어느 입도로든 락을 켜면 **0%** 다: workspace 스코프에서 181개 중 0개, conflict 스코프에서 180개 중 0개. 병합 단계도, 충돌 대화상자도, 롤백 기계도 없다. 동시 동일-파일 쓰기가 물리적으로 결코 일어나지 않기 때문이다. 이것이 락이 장식이 아니라 하중을 받고 있음을 보이는 절제다.

**이 팔이 실모델을 쓸 수 없는 이유.** 이 절제는 두 writer 와 락을 직접 구동하는데, 여기서 그것은 편의가 아니라 질문을 던지는 유일한 방법이다. 위반은 두 쓰기가 물리적으로 겹치는 동안에만 일어날 수 있으므로, 위반 *비율*을 재려면 표본을 뽑을 만큼 자주 겹치는 쓰기가 필요하다. §6.4 의 실모델 측정이 그것이 에이전트 루프를 통해서는 도달 불가능한 이유를 보인다: 실제 턴은 4분에 흩뿌려진 밀리초 미만의 쓰기 여덟 번을 만들고(효과 시간 비중 10⁻⁵), 두 턴이 *같은* 파일을 대상으로 해도 실측 락 대기가 0.1 ms 다. 즉 그들의 쓰기는 사실상 겹치지 않는다. 이 실험의 분류된 동시 쓰기 스냅샷 110개를 실모델로 재현하려면 수천 턴 규모가 필요하고, 그러고도 나오는 비율은 겹쳤을 때 무슨 일이 일어나는가가 아니라 얼마나 드물게 겹쳤는가에 지배된다. 겹침을 강제하고 락이 버티는지 묻는 것이 I/O 경쟁을 재는 표준적 방법이며, 이는 두 질문을 깔끔하게 분리한다: §6.4 는 실제 작업에서 동시 효과가 *얼마나 자주* 충돌하는지를 재고(드물게), §6.2 는 *충돌했을 때 무슨 일이 일어나는지*를 잰다(락 없으면 손상, 있으면 없음). 이 실험이 뒷받침하는 주장은 구성상 조건부이고, 그 조건을 정량화하는 것이 다른 실험이다.

메커니즘을 명명할 가치가 있는데, 그것이 주장의 경계를 정하기 때문이다. 여기서 쓰기는 truncate 후 하나의 큰 `write(2)` 이며 좁은 인터리브 창을 남긴다. 위반율은 그 창의 속성이다: 쓰기를 청크로 스트리밍하는 실행기를 가진 플랫폼은 창을 넓히고, 완전 원자적 교체를 하는 플랫폼은 창을 닫는다. 불변인 것은 방향과 0 이다. 그래서 우리는 이 비율을 이 플랫폼에서 위험이 실재한다는 증거로, 0 을 계약의 보장으로 보고하며, 8.2% 를 보편 상수로 제시하지 않는다.

### 6.3 RQ4a: 병렬 이득은 어디서 붕괴하는가 ✔

정직하려면 경계가 필요하고, 그 경계는 직관이 두는 곳에 있지 않다. 자연스러운 두려움은 "턴이 파일을 쓰면 병렬성은 죽는다"이다. 파일 작업을 전혀 하지 않는 두 동시 턴은 유효 병렬도 1.99 에 이르고, 실제 파일 쓰기를 더해도 중앙값 락 대기는 **모든 셀에서 1 ms 미만**이며 락 스코프 간에도, 같은 파일과 다른 파일 대상 간에도 차이가 없다. 실제 파일 쓰기는 마이크로초에서 밀리초 단위의 효과이고 경합하기에는 너무 짧다. 쓰기의 단순한 *존재*는 경계가 아니다.

우리는 그 그리드를 과대 해석하는 대신 한계를 밝힌다. 그리드가 의도한 두 번째 축, 즉 턴당 쓰기 *횟수*(0, 2, 6)는 사실 작업량을 바꾸지 못했다: 쓰기 2회와 6회에서 턴 지속시간이 동일하다(중앙값 2,897 ms 와 2,898 ms). 목 모델이 *공유* 트랜스크립트에서 가장 최신인 `[[bench …]]` 지시자를 해소하므로 동시 턴들이 하나의 스크립트로 붕괴하기 때문이다. 이는 §6.2 와 §6.4 가 모델 계층을 통째로 우회하게 만든 바로 그 함정이며, 따라서 이 그리드에서는 위의 락 대기 결과만 읽어야 한다.

이득을 실제로 붕괴시키는 것은 **배타 효과의 시간 비중**이고, 두 독립 측정이 그에 동의한다. 각 턴이 자기 구간의 약 50% 를 배타 셸 스텝 안에서 보내면 유효 병렬도(유용 작업에서 락 대기를 제외)는 1.59 로 떨어지고, 약 90% 에서는 **1.10** 에 이른다. 락을 직접 구동하고 효과 비중을 호스트에서 캘리브레이션하는 §6.4 의 통제 실험이 같은 법칙을 반대 방향에서 재현한다: 전역 락 하에서 유효 병렬도는 턴 2개에 대한 해석적 상한 2 / max(1, 2s)(효과 비중 *s*)를 측정한 모든 비중에서 1.3% 이내로 따라간다(측정된 *s* 가 0.23, 0.45, 0.60, 0.73 일 때 예측 2.00, 2.00, 1.68, 1.38 대 실측 1.99, 1.97, 1.66, 1.37).

설계 공간의 진술: *붕괴 경계는 효과 횟수나 충돌률이 아니라 배타 효과의 시간 비중을 따른다.* 실제 코딩 에이전트 턴은 추론이 지배하고 파일 효과는 짧으므로 일반적인 경우는 경계에서 멀리 있으며, 효과가 무거운 영역에서 락에 남는 일은 지연이 아니라 무결성(§6.2)이다.

### 6.4 RQ4b: 충돌 단위 락이 되찾는 것, 그리고 실제 작업이 앉아 있는 자리 ✔

§4.4 는 전역 뮤텍스가 서로소 경로를 건드리는 턴들까지 직렬화하므로 너무 뭉툭하다고 주장한다. 이 주장을 세우려면 측정이 둘 필요하다. 어느 하나만으로는 답이 되지 않기 때문이다. **통제 스윕**은 뭉툭한 락의 비용이 효과 시간 비중에 따라 어떻게 변하는지를 세운다. **실모델 실행**은 그 곡선 위 어디에 실제 작업이 앉아 있는지를 세운다. 두 번째가 없는 첫 번째는 작동점을 못 찍은 법칙이고, 첫 번째가 없는 두 번째는 "락 스코프는 중요하지 않다"와 "이 워크로드가 아직 중요해지지 않는 자리에 있을 뿐이다"를 구분하지 못한다.

**통제 스윕.** 두 턴이 락 없는 사고 구간과 락 걸린 쓰기를 번갈아 수행하되, 각 락 스코프 하에서, 쓰기 대상을 *서로소* 경로 또는 *같은* 경로로 향하게 한다. 사고 구간은 이 호스트에서 쓰기 한 번이 실제로 얼마나 걸리는지의 실행 시점 캘리브레이션(이번 실행에서 1 MiB 에 26.39 ms)으로부터 정해지므로, 효과 시간 비중이 바라는 속성이 아니라 통제된 축이 된다. 이 팔은 §5 에 적은 이유로 도구와 락을 직접 구동하며, 그 이유는 가정이 아니라 실측했다. 셀당 5회, 총 120회.

| 효과 시간 비중 | 경로 | `workspace` (전역) | `conflict` (우리) | 회복 | `off` (참조) |
|---|---|---|---|---|---|
| 25% | 서로소 | 1.987 | 1.992 | 1.003× | 1.994 |
| 25% | 동일 | 1.988 | 1.988 | 1.000× | 1.994 |
| 50% | 서로소 | 1.974 | 1.983 | 1.005× | 1.986 |
| 50% | 동일 | 1.978 | 1.976 | 0.999× | 1.988 |
| 75% | 서로소 | 1.662 | 1.977 | **1.19×** | 1.988 |
| 75% | 동일 | 1.656 | 1.653 | 0.998× | 1.987 |
| 90% | 서로소 | 1.368 | 1.983 | **1.45×** | 1.992 |
| 90% | 동일 | 1.369 | 1.368 | 0.999× | 1.991 |

*동일* 경로 행을 먼저 읽으라: 그것이 대조군이고, 경로가 실제로 충돌할 때 두 스코프가 구별되지 않음을 보인다(1.000×, 0.999×, 0.998×, 0.999×). 따라서 서로소 경로에서의 이득은 보장을 약화시켜 산 것이 아니다. 그것은 애초에 정렬이 필요 없던 작업을 직렬화하던 비용을 되돌려받은 것이다. 전역 뮤텍스 하의 중앙값 락 대기가 스윕 전체에서 같은 이야기를 한다: 28.9 ms, 26.6 ms, 553 ms, 1,077 ms. `off` 열은 남은 오버헤드의 경계를 짓는다: 충돌 단위 스코프는 락이 전혀 없는 경우의 1% 이내로 도는 반면, §6.2 는 `off` 가 무결성에서 무엇을 치르는지 보인다.

아래 두 행은 해명해서 넘길 널 결과가 아니라 효과의 *모양*이다. 턴이 둘일 때 효과를 완전히 직렬화했을 때의 병렬도 상한은 효과 비중 *s* 에 대해 2 / max(1, 2s) 이므로, *s* ≤ 0.5 에서는 전역 뮤텍스가 완벽하게 뭉툭해도 비용이 0 이다. 한 턴의 사고 구간이 다른 턴의 효과 뒤에 통째로 숨기 때문이다. `workspace` 열은 측정한 모든 비중에서 그 상한을 1.3% 이내로 따라간다(§6.3). 즉 뭉툭한 락의 비용은 튜닝으로 없앨 상수 오버헤드가 아니라, 그 선 아래에서는 0 이고 위에서는 효과 비중과 함께 자라는 것이다.

**실제 작업이 앉아 있는 자리.** 스윕이 측정한 가장 낮은 비중은 0.23 이다. 실제 작동점을 찾기 위해 같은 비교를 목도 우회도 없이 돌렸다: 실 온프레미스 모델이 진짜 턴을 구동하고, 두 사용자가 각각 네 개의 파일을 쓰도록 지시받으며, 각 락 스코프와 각 경로 조건에서 총 12회. 효과 비중은 하네스가 아니라 **서버 자신의 락 계측**(보유 시간을 스레드로 턴에 귀속)에서 도출한다.

실측된 작동점은 **효과 시간 비중 1 × 10⁻⁵** 이고, 12회 전체에서 중앙값과 최댓값이 같다: 263 초짜리 턴 구간에 대해 락 보유 3.4 ms. 락 대기는 중앙값과 최댓값 모두 0.1 ms 였다. 말할 만한 스코프 차이가 없으며(서로소: workspace 1.864 대 conflict 1.854; 동일: 1.873 대 1.890), 더 눈에 띄는 것은 동일 경로 결과다: *같은* 네 파일에 쓰는 두 턴이 측정 가능한 경합을 전혀 만들지 않는다. 4분에 흩뿌려진 밀리초 미만의 쓰기 여덟 번은 사실상 겹치지 않기 때문이다.

이 "차이 없음"은 얼버무림이 아니라 측정이며, 정확하게 만들 수 있다. 락 대기가 0 이면 makespan 은 긴 쪽 구간과 같으므로 지표는 항등적으로 1 + 짧은턴/긴턴 으로 환원된다. 12회 **전부**에서 실측값이 그 항등식과 **0.0004** 이내로 일치한다. 실제 시스템이 잃는 병렬도는 남김없이 턴 길이 비대칭이고(실모델에서는 이 편차가 크다. 구간이 198 초에서 413 초까지 퍼졌다), 락의 기여는 그 분해능 아래에 있다.

**이 쌍이 세우는 것.** 법칙은 뭉툭한 락의 비용이 효과 비중 50% 아래에서 0 이라고 말하고, 실 시스템은 그보다 네 자릿수 아래인 10⁻⁵ 에 앉아 있다. 따라서 결론은 "락 스코프는 중요하지 않다"가 아니라 더 날카로운 **"이 워크로드는 무릎보다 한참 아래에 있고, 무릎은 여기다"** 가 된다. 이것은 실모델 실행이 통제 실험을 **대체할 수 없는** 이유도 설명한다: 턴 길이 편차 ±100 초가 0.1 ms 의 락 대기를 압도하므로, 실측은 락의 기여에 상한을 씌울 수는 있어도 그 안의 차이를 분해할 수는 없다. 한 실험은 위치를 찍고, 다른 실험은 분해한다.

부수적 관찰 하나를 기록해 둘 가치가 있는데, §6.7 을 확장하기 때문이다. 실모델 12회 중 1회에서 한 턴이 **다른 사용자의 과제**를 수행해 자기 파일 대신 그 사용자의 파일을 썼다. 이는 §6.7 의 공유 트랜스크립트 의미 혼동이 답변 텍스트가 아니라 **부수효과**에서 나타난 것이고, 의도적으로 적대적인 모델이 아니라 실모델에서 나타난 것이다. 이 비율은 일반 추정치가 아니다: 여기서 두 동시 지시는 태그와 파일명만 다르므로 혼동에 가장 유리한 조건에 가깝다. 이것이 세우는 것은 **존재**이며, 위의 경로 축 비교가 의도한 경로 조건을 실제로 구현한 실행만 세는 이유이기도 하다(효과 비중과 락 대기는 전 실행을 쓴다. 어느 쪽이든 쓰기가 일어났고 같은 락을 통과했다).

### 6.5 RQ5: 재접속하는 클라이언트는 같은 세션에 도달하는가 ✔

한 번도 끊기지 않는 클라이언트만 서비스하는 공유 세션은 어떤 유용한 의미로도 공유가 아니다. 사람들은 노트북을 닫고, 네트워크를 바꾸고, 늦게 합류한다. §4.7 은 `seq` 와 `Last-Event-ID` 로 이를 설계했고, 이 실험은 설계가 실제로 그것을 주는지, 같은 라이브 세션에 두 구독을 돌려 대조함으로써 묻는다.

세 사용자가 병렬 계약으로 90턴을 흘렸다. 한 구독(*control*)은 내내 붙어 있었다. 두 번째(*cutter*)는 11회 끊었다 다시 붙으며, 매번 마지막으로 받은 id 를 브라우저의 `EventSource` 가 하듯 그대로 제시했다. 절단은 타이머가 아니라 이벤트 수로 촉발돼 모든 절단이 트래픽 뒤가 아니라 그 안에 떨어지게 했다.

두 구독은 **동일한** 스트림을 받았다: 각각 180개의 재생 가능 이벤트가, 시퀀스 번호·이벤트명·**페이로드 원문**까지 같았고, 누락 0, 중복 0, 순서 역전 0, 페이로드 불일치 0 이었다. 제출한 90개 질문 마커가 모두 양쪽에 도달했다. 대조가 페이로드 원문을 포함하므로 턴 귀속은 결과 옆이 아니라 결과 안에 있다: 재생된 이벤트가 뭉개지거나 빠진 `reply_to` 를 실었다면 불일치가 됐을 것이다. 11회의 정상 재접속 중 어느 때도 리셋이 발신되지 않았고, 그것이 증분 재생의 요점이다: 클라이언트의 트랜스크립트는 다시 지어진 것이 아니라 이어졌다.

이어서 서버가 살릴 수 없는 커서로 폴백 경로를 강제했다. 다른 스트림 epoch 의 커서(재개 후 재기동 경우)와 발급한 적 없는 앞선 커서 둘 다 같은 답을 냈다: 연결 정체성 바로 뒤, 재생되는 모든 이벤트보다 앞인 두 번째 프레임에 리셋 신호가 오고, 180개 전체의 스냅샷이 뒤따랐다. 세 번째 살릴 수 없는 경우, 즉 보존 창보다 오래된 커서는 같은 분기로 합류하지만 출하 버퍼 크기에서 약 1,500턴을 쌓아야 도달하므로 이 하네스가 아니라 유닛 테스트가 덮는다.

### 6.6 RQ6: 턴이 도는 동안 공유 컨텍스트 압축하기 ✔

공유 컨텍스트는 단일 사용자의 것보다 N 배 빨리 찬다. 그래서 여기서 압축은 선택이 아니다. 그리고 압축은 선행 연구가 답하지 않은 질문을 제기하는데, *동시 사용자 턴이 덧붙이는 도중에* 대화를 압축한 선행 연구가 없기 때문이다(§2.4). 설계는 낙관적이다: 락 아래에서 이력을 분할하고 세대 번호를 기록하고, 락 밖에서 요약하고(수 초짜리 LLM 콜), 그다음 세대를 재검증해 원자적으로 커밋하면서 그동안 동시 턴이 덧붙인 꼬리를 흡수한다.

강제 압박 하에서 측정했다(3 사용자 × 30 라운드, 90턴, 의도적으로 작게 광고한 컨텍스트 창과 800 ms 요약기). 네 번의 압축 모두 3개의 턴이 비행 중인 상태에서 시작했고, 턴들은 무락 요약 구간 *안에서* 계속 흘렀다. 그 구간들에 걸쳐 42개의 턴 이벤트가, 그중 11개의 첫 토큰이 떨어졌는데, 이는 배리어 설계라면 구성상 0 으로 만들었을 가용성이다. 90개 질의 중 0개가 유실됐고, 90개 최종 레코드 전부가 `reply_to` 를 통해 실재하는 질문으로 해소되어 구조적 귀속이 모든 패스를 견뎠다. 네 번의 압축 모두 stale 재시도 0 으로 커밋됐다.

그 결과에 이르기 전에 계측이 실제 설계 결함도 잡았다. 첫 구현에서는 동시 턴의 초과 폴백(창을 넘으면 가장 오래된 메시지를 축출)이 모든 요약 구간마다 세대를 올려 낙관적 커밋을 굶겼다: 6번 중 1번만 안착했고 stale 재시도가 5회였으므로, 요약값은 지불되고 버려지는 한편 원시 이력은 요약되지 않은 채 축출됐다(`out/n1-compaction-adversarial.json` ✔). 수리는 압축이 비행 중인 동안 동시 호출자가 예방 목표와 진짜 한계 사이의 여유를 감내하게 하는 것이다. 이 실패를 보고하는 이유는 그것이 단일 사용자 시스템에서는 발견될 수 없는 종류의 상호작용(압축 × 동시성)이기 때문이고, §5 의 계측 평면이 그것을 찾아냈기 때문이다.

### 6.7 RQ7: 휴리스틱이 아니라 구조적인 귀속 ✔

동시성 하에서 "이 답은 어느 질문의 것인가"는 틀리기 쉽고 흉내내기도 쉽다. 가장 가까운 선행 메시지 휴리스틱은 동시성이라 할 만한 것이 없을 때 정확히 맞는다. 이 시스템은 귀속을 *구조적*으로 만든다: 한 턴이 한 스레드이고, 야기한 질문의 정체성이 스레드 로컬 상태에 살며(§5), 그 질문을 처리하는 동안 쓰이는 모든 레코드는 모델이 뭐라 말하든 자기 `reply_to` 를 지닌다.

대규모에서 검증했다(4 사용자 × 25 라운드, 100개 동시 턴). 발급 사슬(큐 진입 → 디스패치된 턴 → 스레드 → 발급된 질문 id)이 100/100 정확했고, 최종 레코드들은 질문들과 완전한 전단사를 이뤘다: 중복 0, 미대응 0, 누락 0.

같은 실험이, 우리가 아는 한 처음으로, 구조적 귀속이 의도적으로 풀지 않는 *의미적* 현상을 정량화한다. 턴들은 하나의 트랜스크립트를 공유하므로 모델은 자기 턴이 서비스하는 질문이 아닌 질문에 답할 수 있다. 공유 트랜스크립트에서 보이는 가장 최신 질문에 *항상* 답하는 결정적 적대 모델로 구동했을 때, 턴의 38% 가 다른 사용자의 나중 질문을 *내용상* 다룬 답을 내놓았고, 그동안 `reply_to` 는 그 턴이 어느 요청을 서비스하고 있었는지를 여전히 정확히 기록했다. 이는 자주 뒤섞이는 두 주장을 깔끔하게 분리한다: *누구의 턴이 이것을 만들었는가*(구조적, 이제 정확)와 *모델이 누구의 질문에 답하기로 했는가*(하나의 트랜스크립트를 공유하는 것의 속성이며, 여기서 최악 케이스로 경계 지어졌다. §6.10 의 실모델 실행은 3/3 으로 자기 질문자에게 답했다). 더 가벼운 부하는 같은 분리를 더 낮은 의미적 비율로 재현한다: §6.6 의 압축 실행에서 90개 중 4개였고, 구조적 귀속은 그때도 완전했다.

### 6.8 RQ8: 폭주 하의 공정성 ✔

한 사용자가 긴 턴 다섯 개를 연달아 제출하고, 다른 넷이 각각 짧은 질문 하나씩을 던진다(상한 = 3, 5회 반복). 사용자별 게이트를 켜면(사용자당 활성 턴 하나, 대기자는 다른 사용자를 위해 건너뜀) 짧은 질문은 중앙값 **76 ms**(p95 148 ms)에 디스패치됐고 폭주자 자신의 백로그는 자기 뒤에서 기다렸으며(중앙값 3.3 s), 사용자별 단일 활성 불변식은 위반 0 으로 지켜졌다. 게이트를 절제하면(순수 FIFO + 상한) 폭주자가 슬롯을 독점했고 같은 짧은 질문들이 중앙값 **1,807 ms**(p95 3,219 ms)를 기다렸다. 24× 나쁘며, 단일 활성 불변식 위반이 20건이었다.

*모든* 사용자에 대해 계산한 순진한 Jain 지수는 절제한 팔에서 오히려 *더 높다*(0.85 대 0.24): 모두가 똑같이 나쁘게 서비스된다. 게이트는 폭주자의 백로그에 대해 의도적으로 "불공정"하며, 그 비대칭이 결함이 아니라 메커니즘 자체다. 그래서 계층별 절대 대기가 정직한 보고이고 스칼라 공정성 지수가 오도하는 쪽이다.

### 6.9 RQ9: 세션 수명주기(중단, 재개, 유계 유지) ✔

공유 세션은 서버 재기동과 수백 턴을 가로질러 *지속*될 때만 유용하다. 세 사용자가 하나의 세션에서 네 단계에 걸쳐 204턴을 몰았고, 단계 사이에 서버 프로세스를 종료하고 `--resume` 으로 재기동했다(3회 중단/재개).

모든 검사가 통과했다. 최종 단계 후 durable 이력에 204개 질의 중 204개가 존재했고, 재기동을 가로지른 유실은 0 이었다. 질문 id 는 재개를 가로질러 전역 유일로 남았는데, 재개 경로가 id 카운터를 압축된 캐시가 아니라 *전체* 이력에서 재도출하기 때문이다. 그래서 재기동된 세션이 압축은 작업 창에서 떨어뜨렸으나 기록에는 귀속이 남아 있는 id 를 다시 발급하지 않는다. 재개된 각 단계는 새 51턴을 정상 처리했다. 압축은 공유 컨텍스트를 내내 유계로 유지했다: 실행 전체에서 10회 커밋(stale 2, 실패 0)이었고, 압축 후 캐시는 2,610 토큰을 넘은 적이 없다. 세션의 컨텍스트 발자국은 세션 나이가 아니라 압축기가 다스린다.

### 6.10 RQ10: 실모델 검증과 병렬성의 진짜 값 ✔

위의 모든 수치는 결정적 목 지연을 쓴다. 우리는 대표 결과를 스팟체크하고 비용 문제를 실 온프레미스 모델로 정산했다(Qwen3.6-27B, MLX 8비트; 순위 확인 5회, 토큰 회계 3회).

*순위 보존.* 사용자 A 가 긴 생성을 시작하고 B 가 2 s 뒤 한 줄을 묻는다. B 의 TTFT: 직렬 중앙값 **27.8 s**(B 가 A 의 턴 전체를 기다린다), 병렬 중앙값 **7.0 s**. 순위가 재현되며, 병렬 절대값은 실 서버가 무엇을 더하는지에 대해 정직하다: B 의 7 s 는 하네스의 0.29 s 바닥이 아니라 실제 prefill 에 공유 추론 하드웨어에서의 동시 디코드 감속이 더해진 것이다. A 의 작업 *길이*로부터의 독립은 성립한다(§6.1). A 의 *존재*로부터의 독립은 제공자의 동시성이라는, 세션 계약이 만들어낼 수 없는 서빙 계층 속성에 의해 경계 지어진다.

*토큰 회계.* 명백한 두려움은 N 개의 병렬 턴이 약 N 배의 추론 비용을 치른다는 것이다. 동일한 세 질문 워크로드에서 측정: 병렬 계약은 3콜에 **23.2K** 입력 토큰, 직렬 계약은 **2**콜에 **15.6K** 로, 3× 가 아니라 **1.49×** 프리미엄이다. 두려움과 측정 사이의 간극은 두 메커니즘이 설명한다. 첫째, 콜당 입력은 고정 시스템 프롬프트가 지배하므로(여기서는 각 콜의 약 7.7K), 비용은 스냅샷 재독이 아니라 *콜 수*를 따른다. N× 두려움을 낳은 공유 컨텍스트 중복은 이력이 길어지기 전까지는 부차항이다. 둘째, 직렬 계약은 조용히 *배치*한다: 실행 중 주입이 큐에 든 질문을 도는 턴 안으로 접는다(질문 3개, 콜 2회). 이는 병렬성이 포기하는 실질적 효율이며, 질문당 턴 하나가 바로 §6.1 의 지연 독립성을 사는 것이기 때문이다. 거래의 정직한 진술: 병렬성의 토큰 프리미엄은 콜 수의 비(최대 N, 부하가 걸려 직렬 배치가 실패할수록 줄어든다)이고 지연 독립성의 대가로 치러진다. 이 배포에는 없는 제공자 측 프롬프트 캐싱이 고정항을 더 줄일 것이다.

### 6.11 정확성과 타당성 위협

동시성 기계는 리포지토리의 테스트 스위트가 덮는다: 117개 파일에 걸친 3,534개 테스트 중 3,498개가 통과하고, 35개는 환경 게이트로 건너뛰며, 1개는 아래에 적은 알려진 플레이크다. 커버리지에는 턴 레지스트리와 사용자별 게이팅, 효과 락 스코프와 호환성 행렬, 컨텍스트 스냅샷과 원자 커밋, 시퀀스 번호와 증분 재생, 읽기 전용 토큰의 엔드포인트별 권한 표(등록된 모든 라우트에 대해 파라미터화되므로, 어느 쪽인지 선언하지 않은 새 엔드포인트는 기본 개방되는 대신 스위트를 깨뜨린다), 중단/재개가 포함된다. 별도의 opt-in 스위트 53개 테스트가 실제 헤드리스 브라우저로 실제 서버를 구동해 유닛 테스트가 닿을 수 없는 행동을 검사한다. 예컨대 속성으로 숨긴 요소가 저자 스타일시트가 적용된 뒤에도 실제로 숨겨지는가 같은 것이다. 이 스위트는 이번 작업 중 소스 수준 단언은 통과시킨 읽기 전용 클라이언트의 실제 결함을 잡아냈다. 유일한 실패는 코드 인덱스 구성요소에 대한 속성 기반 테스트로, 부하 하에서 hypothesis 데드라인이 만료되는 알려진 플레이크이며 관례상 회귀 산술에서 제외되고 동시성 계약과 무관하다.

**타당성 위협.** 목 LLM 타이밍은 제공자 측 분산을 배제하며, §6.10 의 실모델 스팟체크와 둘 다 존재하는 곳에서 결정적/실측 쌍을 함께 보고함으로써 완화한다. 측정은 하나의 호스트, 하나의 파일시스템에서 나왔다. §6.2 는 자기 수치 중 무엇이 플랫폼 속성이고 무엇이 계약 보장인지 명시하고, §6.1 은 세션 경계를 넘는 유일한 지점을 명시한다. §6.10 의 토큰 프리미엄은 짧은 이력 워크로드에서 측정됐으며 이력 길이와 직렬 배치 실패에 따라 커진다. 우리 워크로드는 현장 로그 혼합 분포가 나오기까지 합성이다 [TODO: S1]. 마지막으로 이것은 시스템 평가다: 계약이 구현 가능하며 무엇을 치르는지를 세우지, 이를 쓰는 팀이 더 잘 협업한다는 것을 세우지 않는다. 후자는 §7 이 준비하는 사용자 연구다.

---

## 7 논의: 세션 공유가 만들어내는 것

계약은 위임의 구조적 한계를 제거한다. 거기서 멈추는 것은 부정직할 것이다: 하나의 라이브 세션에서의 공존은 분리된 위임에는 없던 문제를 만든다. 우리는 §2.5 의 이론에서 넷을 도출한다. 이들은 본 논문의 해결된 주장이 아니라 연구 의제다.

### 7.1 비대칭 그라운딩

Clark 과 Brennan 의 그라운딩 설명 [32] 은 당사자들이 공통 기반을 *함께* 축적한다고 전제했다. 지속되는 공유 에이전트 세션은 그 대칭을 깬다: 사용자 A 와 에이전트가 30분치 작업 컨텍스트(내려진 결정, 포기된 접근, 건드린 파일)를 쌓고 사용자 B 가 차갑게 붙는다. B 는 트랜스크립트를 재생할 수 있고 §6.5 가 그 재생이 정확함을 보이지만, 재생은 접근이지 이해가 아니다. 세션의 공통 기반은 이제 소유 기울기를 가진 인공물이다. 우리의 시퀀스 순서·턴 귀속 트랜스크립트는 따라잡기 인터페이스(요약, 결정 다이제스트, 파일별 "여기서 무슨 일이 있었나" 뷰)의 원재료지만, 그중 무엇이 간극을 닫는지, 그리고 그것을 닫는 것이 언제나 바람직하기는 한지는 열린 실증 문제다(계획 연구 S2: 세 가지 따라잡기 처치 하의 단계적 세션 중간 합류에서 이해도, 첫 생산적 개입까지의 시간, 정리된 결정의 재논의를 측정).

### 7.2 다자 제어권 중재

Horvitz 의 혼합 주도 원칙 [36] 은 에이전트에게 언제 행동하고 언제 물어야 하는지 말해 주지만 한 인간을 전제한다. 여럿이면 모든 제어 동사가 간접 목적어를 얻는다: *누구의* 승인, *누구의* 인터럽트, *누구의* 되돌리기? 우리가 출하한 정책은 의도적으로 최소다: 사용자별 턴 소유권(당신 것은 인터럽트할 수 있고, 내 것은 건드릴 수 없으며, 내 턴이 당신의 입력창을 잠그지 않는다)과, 행동 없이 지켜봐야 하는 참여자를 위한 읽기 전용 역할(§5). 이는 역할 기반 override(사고 지휘자가 무엇이든 멈춘다), 파괴적 효과에 대한 정족수 게이트, 사회적 투명성 접근(누구든 무엇이든 멈출 수 있되 가시적으로)도 담고 있는 정책 공간의 한 점이다. 단독 사용자가 39% 의 경우 브레이크를 밟는다는 SWE-chat 의 발견 [3] 이 질문을 날카롭게 한다: 공유 세션에서 제동은 잉여인가(누군가 잡을 것이다), 확산되는가(모두가 남이 하겠지 한다, 방관자 패턴), 증폭되는가(눈이 많으면 브레이크도 많다)? 턴 id 메커니즘은 이 정책들 어느 것이든 구현 가능하게 만든다. 그중 무엇이 *작동하는지*는 연구 S3 다.

### 7.3 에이전트 속도에서의 동료 책임성

Daryanto 등은 동료가 당신의 AI 사용을 볼 수 있을 때 적용 전에 제안을 더 검증한다는 것을 발견했다 [24]. 제안 기반의, 인간 속도 설정에서였다. 자율 에이전트는 체제를 바꾼다: 효과가 여러 파일에 기계 속도로 떨어지고, 동료가 관찰하는 것은 커서 아래의 diff 가 아니라 스트림이다. 사람이 읽을 수 있는 것보다 분당 검증할 것이 많을 때도 가시성이 여전히 검증을 사는가? 우리의 팬아웃 설계는 모든 참여자를 기본적으로 잠재적 검증자로 만들고, 읽기 전용 역할은 순수 관찰자를 일급 참여자로 만든다. 그것이 Daryanto 의 책임성을 낳는지 단순한 경보 피로를 낳는지는 이 시스템에서 정확히 시험 가능하며 아직 알려지지 않았다.

### 7.4 접합 작업은 파괴되지 않고 이동한다

Schmidt 와 Bannon 은 조정 기술이 접합 작업을 제거하기보다 재배치한다고 예측했다 [35]. 위임의 접합 작업은 현장에서 보인다: 어느 채널이 어느 작업을 띄웠는지 추적하기, 병행 멘션 중복 제거하기, 각각 버그를 절반씩 푸는 PR 세 개 조정하기. 공유 세션은 이 중 많은 것을 메커니즘으로 흡수한다: 큐가 순서를 매기고, 락이 직렬화하며, `reply_to` 가 귀속시킨다. 그 자리에 새 작업이 나타난다: 도는 턴에 조타를 주입할 때를 정하기, 상한을 협상하기, 동시 스트림의 헤엄을 읽기. 우리는 이 교환이 유리하다고 추측하지만(기계화된 순서 매기기가 사회적 순서 매기기보다 싸다), 이론과 함께 정확한 주장은 *이동*이라는 것, 그리고 옛 작업의 소멸을 축하하는 것보다 새 작업을 측정하는 것(S1 현장 로그)이 더 중요하다는 입장을 유지한다.

### 7.5 한계

현재 시스템과 결과의 정직한 경계. (1) *토큰 비용, 측정됨*(§6.10): 동일한 세 질문 워크로드에서 병렬 계약은 직렬 계약 입력 토큰의 1.49× 를 치렀다(3콜 대 2콜; 직렬은 실행 중 주입으로 큐에 든 질문을 도는 턴에 배치하는데, 이는 병렬성이 지연 독립성을 위해 의도적으로 포기하는 효율이다). 프리미엄은 컨텍스트 재독이 아니라 콜 수를 따르고 콜당 입력은 고정 시스템 프롬프트가 지배한다. 직렬 배치가 완벽하고 이력이 길어야만 N× 에 접근하며, 제공자 측 프롬프트 캐싱(우리 배포에는 없다)이 고정항을 더 줄일 것이다. (2) *컨텍스트 용량, 부분 해결*: 하나의 공유 컨텍스트는 N 배 빨리 찬다. 토큰 예산 압축이 동시 턴과 통합되고 측정됐다(§6.6): 가용성이 유지되고 레코드 유실이 없되, 지속적 초과 하에서 이따금 낭비되는 요약 콜이 비용이다. 장기 용량 한계(N 명의 지속 사용자가 몇 시간에 걸쳐 하나의 창에 무엇을 하는가)는 미측정으로 남는다. (3) *논리적 last-wins*: 직렬화는 물리적 오염을 막지 의미적 충돌을 막지 않는다. 두 턴이 파일 내용에 대해 이견을 가질 수 있고 나중 것이 이긴다. 의미적 충돌을 인간에게 드러내는 것은 열려 있다. 공유 트랜스크립트는 또한 턴이 다른 사용자의 요청을 다루게 할 수 있다. *답변*에 대해서는 적대적 always-answer-latest 모델 하에서 최악 38% 로 정량화됐고(§6.7), *부수효과* 수준에서는 실모델로 관측됐다: 한 턴이 다른 사용자의 파일을 썼다(12회 중 1회, §6.4). 구조적 귀속은 그 턴이 어느 요청을 서비스하고 있었는지 기록하지만, 서비스하는 동안 모델이 무엇을 하는지를 제약하지는 않는다. (4) *락 봉투*: 도구가 백그라운드로 분리한 프로세스의 효과는 직렬 실행기를 벗어난다. (5) *스냅샷 staleness*(§4.3). (6) *단일 인스턴스*: 이벤트 버스·락·레지스트리가 프로세스 내에 있어 세션이 여러 머신에 걸치지 않는다. (7) *격리 깊이*: 컨테이너 수준이 아니라 디렉토리 수준 경로 감금이다. 신뢰할 수 없는 참여자를 받는 배포에는 이 시스템이 주장하지 않는 격리가 필요하다. (8) *인터럽트를 동반한 조타*는 동시 턴에서 미지원이다(순수 취소만). (9) *재생 창*: 증분 재생은 유계 버퍼 안에서 정확하다(§6.5). 창보다 오래 자리를 비운 클라이언트는 명시적 안내와 함께 정확하되 잘린 트랜스크립트를 받고, 완전한 기록은 디스크에 남는다.

---

## 8 결론

배포된 모든 코딩 에이전트는 싱글플레이어이고, 업계의 팀 우회로인 @멘션 위임은 팀이 협업자에게서 가장 필요로 하는 두 가지를 구조적으로 차단한다: 작업이 일어나는 동안 개입할 능력, 그리고 두 사람이 동시에 말할 때 무슨 일이 일어나는지에 대한 규칙. 통념은 이를 공유 세션 안에서 고치려면 직렬화나 포크를 강요당한다고 보았다. 우리는 그렇지 않음을 보였다. 응답성과 무결성을 서로 다른 계층에 두면(추론은 병렬이고 결코 막히지 않으며, 부수효과는 직렬이고 결코 동시가 아니다) 여러 인간이 진짜로 공유하는 세션이 나온다: 모두의 질문이 자기 속도로 답해지고(TTFT 기울기 0.00 대 1.03), 경쟁으로 오염되는 파일이 없으며(락 해제 8.2% → 0%), 모든 답이 질문자에게 귀속되고(100/100), 재접속하는 클라이언트가 다시 시작되지 않고 이어지며(180개 중 180개 이벤트 동일), 모든 참여자가 남의 것을 건드리지 않고 자기 작업을 멈출 수 있다.

우리는 증거의 값만큼이나 그 *모양*이 중요하다고 생각한다. 이것은 계약을 설계한 뒤 그것을 보여주려고 지은 프로토타입에서 시연한 것이 아니다. Coagora 는 이미 직렬 계약으로 공유 세션을 출하하고 있었고 지금도 그것을 기본값으로 출하한다. 병렬 계약은 그 옆에 스위치로 추가됐고, 그래서 모든 실험의 기준선은 남의 것을 재구성한 무언가가 아니라 시스템 자신의 프로덕션 경로다. 결과는 팔 사이의 유일한 차이가 계약 그 자체인 비교이며, 계약의 비용 또한 그 이득과 같은 계기에서 보고된다: 토큰에서 1.49×, 효과가 무거운 영역에서 유효 병렬도 1.10 까지, 그리고 락 입도가 더 이상 중요하지 않게 되는 경계(효과 비중 50% 아래).

풀지 못한 것은 이름을 붙였다. 공유 세션은 비대칭 그라운딩을 주조하고, "자기 턴을 소유하라"보다 풍부한 제어 중재 정책을 요구하며, 동료 책임성이 기계 속도에서 살아남는지를 시험한다. 이들은 멀티플레이어 에이전트의 결함이 아니다. 에이전트가 멀티플레이어가 되면 열리는 분야다. 시스템·하네스·원시 데이터는 그 작업을 위해, 우리 것까지 포함해 공개돼 있다.

---

## 참고문헌 (References)

[NOTE: 투고처 형식으로 정규화; ⚠ = 카메라레디 전 원문 대조 필요(연구 문서 04 §9).]

1. *From developer pairs to AI copilots.* arXiv:2506.04785, 2025.
2. V. Chen, A. Talwalkar, J. Brennan, G. Neubig. *Code with me or for me? How increasing AI automation transforms developer workflows.* arXiv:2507.08149, 2025.
3. Baumann et al. *SWE-chat: Coding agent interactions from real users in the wild.* arXiv:2604.20779, 2026.
4. GitHub Next. *Ace: Agent Collaboration Environment.* Technical preview, 2026. https://ace.githubnext.com/ (2026-08-05 접근).
5. *Claude Code issue #60082: multi-user shared sessions.* GitHub, 2026.
6. Anthropic. *What is Claude Tag.* Support documentation, 2026. https://support.claude.com/en/articles/15594475
7. GitHub. *About Copilot integrations* (스레드 포착 고지). Documentation, 2026.
8. LangChain. *LangGraph double-texting strategies.* Documentation.
9. CopilotKit. *OpenTag: threads & persistence architecture* (스레드당 단일 활성 실행; 동시성에 409). Documentation, 2026.
10. 업계 병렬 에이전트 도구(브랜치/워크트리 격리 후 사후 병합). [NOTE: 인용 가능한 대표 사례 1~2개 선정.]
11. *CoAgent*: 공유 상태 위 동시 에이전트를 위한 낙관적 쓰기 정렬 + 사가 방식 수리. [NOTE: 특허 비특허문헌 1 에서 서지 완성.]
12. *DeLM*: 입장 게이트 기반 공유 컨텍스트 병렬 추론. [NOTE: 비특허문헌 2 에서 서지 완성.]
13. S. Imai. *Is GitHub Copilot a substitute for human pair-programming?* ICSE Companion 2022.
14. Anthropic. *Introducing Claude Tag.* 2026. https://www.anthropic.com/news/introducing-claude-tag
15. OpenAI. *Codex — now generally available.* 2026. https://openai.com/index/codex-now-generally-available/
16. OpenAI. *Codex Slack integration.* Documentation, 2026. https://learn.chatgpt.com/docs/third-party/slack
17. GitHub. *Integrate Copilot cloud agent with Slack.* Documentation, 2026. https://docs.github.com/en/copilot/how-tos/use-copilot-agents/cloud-agent/integrate-cloud-agent-with-slack
18. Cognition. *Devin Slack integration.* Documentation, 2026. https://docs.devin.ai/integrations/slack
19. Cursor. *Cursor in Slack.* Documentation, 2026. https://cursor.com/docs/integrations/slack
20. MITRE ATLAS. *Case study AML.CS0035: Data exfiltration from Slack AI via indirect prompt injection.* 2024.
21. Lehmann, Shauchenka, Buschek. *Collaborative document editing with multiple users and AI agents.* CHI 2026. arXiv:2509.11826.
22. *Controlling AI agent participation in group conversations: A human-centered approach.* IUI 2025.
23. ⚠ *GroupMemBench: Benchmarking LLM agent memory in multi-party conversations.* arXiv:2605.14498, 2026.
24. Daryanto et al. *Human-human-AI triadic programming.* arXiv:2601.12134, 2026.
25. *When AI teammates meet code review.* MSR 2026. arXiv:2602.19441.
26. ⚠ Z. Z. Wang et al. *Position: Humans are missing from AI coding agent research.* 2026. [NOTE: PDF 에서 투고처/저자 목록 확인.]
27. M. Liu et al. *Multi-agent collaboration with state management* (STORM: 공유 코드베이스 위 에이전트의 낙관적 동시성). arXiv:2605.20563, 2026.
28. M. Yang et al. *Justitia: Fair and efficient scheduling of task-parallel LLM agents with selective pampering.* arXiv:2510.17015, 2025.
29. M. Cim et al. *Parallel context compaction for long-horizon LLM agent serving.* arXiv:2605.23296, 2026.
30. S. Yang et al. *Multi-user large language model agents.* arXiv:2604.08567, 2026.
31. S. Yang et al. *ProACT: Towards breakdown-aware proactive agents in multi-user collaboration.* arXiv:2607.03730, 2026.
32. H. H. Clark, S. E. Brennan. *Grounding in communication.* In Perspectives on Socially Shared Cognition, 1991.
33. P. Dourish, V. Bellotti. *Awareness and coordination in shared workspaces.* CSCW 1992.
34. C. Gutwin, S. Greenberg. *A descriptive framework of workspace awareness for real-time groupware.* JCSCW, 2002.
35. K. Schmidt, L. Bannon. *Taking CSCW seriously: Supporting articulation work.* JCSCW 1(1), 1992.
36. E. Horvitz. *Principles of mixed-initiative user interfaces.* CHI 1999.

---

## 부록 A: 아티팩트 가용성 (초안)

시스템, 결정적 벤치마크 하네스, 모든 원시 측정 파일이 하나의 리포지토리에 있다. 하네스는 `bench/multiuser/` 아래에 살고 표준 라이브러리만 쓰며, 실제 서버를 HTTP 로 구동해 지연과 도구 스텝이 프롬프트 안 지시자로 스크립트되는 Python 목 LLM 을 향하게 한다. 각 스크립트는 원시 JSONL 과 파생 요약을 `bench/multiuser/out/` 에 쓰고 이는 커밋되므로, §6 의 모든 수치는 아무것도 다시 돌리지 않고 원시 파일에서 재계산할 수 있다.

| 스크립트 | 절 | 필요 조건 |
|---|---|---|
| `e2_hol.py` | §6.1 선행 차단 그리드 (3계약 × 4 작업 길이) | 목만 |
| `e1_ablation.py` | §6.2 락 스코프별 무결성 절제 | 목만 |
| `p2_grid.py` | §6.3 붕괴 경계 (쓰기 횟수, 충돌, 스코프) | 목만 |
| `p2_scope.py` | §6.4 충돌 스코프 회복 (효과 비중 × 경로 × 스코프) | 목만 |
| `p2_scope_real.py` | §6.4 실제 작동점 (실모델, 효과 비중을 락 계측에서 실측) | 온프레미스 모델 엔드포인트 |
| `n4_replay.py` | §6.5 늦은 합류자 증분 재생 | 목만 |
| `n1_compaction.py` | §6.6 동시 턴 하의 압축 | 목만 |
| `n3_attribution.py` | §6.7 대규모 구조적 귀속 | 목만 |
| `p4_fairness.py` | §6.8 사용자별 공정성 절제 | 목만 |
| `p7_lifecycle.py` | §6.9 3회 중단/재개를 가로지른 204턴 | 목만 |
| `p6_real_llm.py` | §6.10 실모델 순위와 토큰 회계 | 온프레미스 모델 엔드포인트 |

§6.4 의 실모델 팔과 §6.10 만 자격증명이 필요하고(환경 변수로 공급되는 OpenAI 호환 엔드포인트), 나머지는 전부 오프라인으로 돈다. 각 조건은 격리된 `HOME` 을 가진 새 임시 워크스페이스에서 실행되므로 벤치마크 실행이 운영자의 설정을 읽거나 수정할 수 없다. `e2_hol.py` 는 `--append` 를 지원해 새로 측정한 작업 길이를 커밋된 원시 파일에 합치고 요약을 합집합에서 재도출한다. `out/e2-drift-control.json` 은 §6 에서 논한 세션 경계에 대한 대조 측정을 기록한다. [NOTE: 이중 맹검 심사를 위해 익명 아티팩트 링크로 교체.]
