# Python
__pycache__/
*.py[cod]
*.egg-info/
dist/
build/
# stray build artifacts (normal builds land in dist/; this guards a root drop)
*.whl
*.tar.gz

# Testing
.pytest_cache/
htmlcov/
.coverage

# IDE
.vscode/
.idea/

# OS
.DS_Store

# Agent-CLI
.agent-cli/*
!.agent-cli/skills/
.claude/
out.txt
perf-kpi.json        # KPI 측정 run 산출물 (베이스라인은 docs/PERF-KPI.md)
OptimizationToDo.md
OptimizationSummary.md

# SWE-bench harness (bench/) — venv, repo cache, run outputs are local
bench/.venv/
bench/.cache/
bench/runs/
logs/
